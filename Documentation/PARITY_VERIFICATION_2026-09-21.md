# CodeRim parity continuation — 2026-09-21

This continues the [2026-09-20 audit](FULL_AUDIT_2026-09-20.md). It records source changes and actual execution evidence. It does not certify 100% functionality, all-provider live authentication, pixel-identical operating systems, or a signed Windows release. Windows widgets remain outside scope.

## Implemented changes

- Firefox cookie import for twenty supported readers, with profile selection, domain/path/expiry/HTTPS scoping, isolated DPAPI storage, validation before replacement, and cancellation/close guards.
- Explicit Amp API/CLI/Web selection with separate API-key and Web-cookie slots, scoped Firefox verification and pinned Svelte Free-quota parsing; Windsurf local-cache sources; Antigravity OAuth/Local IDE selection with process-bound loopback transport. CLI/cache sources do not inherit another account's API quota or restore an unverified snapshot.
- Factory API/Authorization/Cookie input and Firefox sign-in across pinned origins; authentication-mode changes restart profile and billing together, bounded same-profile cookie conflict recovery, strict user-ID matching and 429 stop.
- Factory saved WorkOS session JSON refresh with pinned public clients, strict response fields and same-account checks; rotated tokens commit with version checks and cross-process file locking before quota reads. Replacing/removing an account wins over a late refresh response.
- Groq console JWT/session JSON and Firefox sign-in, pinned Stytch refresh, 30-day activity and dollar costs. Enterprise Prometheus metrics remain a separate source; missing fields remain unknown.
- Today/7D/30D analytics with hourly/daily buckets, model navigation and Back, visible totals, proportional sub-dollar cost bars, unknown-cost gaps, and narrow-window layout.
- Separate two-second Codex/Claude activity polling, bounded backward JSONL scanning, exact prefix verification on append/rewrite, live Claude transcript selection and stable provider-specific activity timing.
- Provider plan/status updates without rebuilding controls, Usage refresh action, tray reveal preference preservation, existing-instance activation IPC, selected-monitor card sizing, and accessible ring state.
- Windows ARM64 CI downloads the exact archive produced by the packaging job, validates its checksum, executes the extracted application and CLI, and records the OS/process architecture. Workflow implementation is distinct from a successful remote run.

## Additional authentication checkpoint

The source now adds GitHub CLI account reuse, trusted GLM local Coding Plan discovery, Codebuff local sign-in and regional Moonshot keys. Copilot discovery follows the same active-account/blank-environment rules on macOS and Windows. DeepSeek API keys and Web sessions have separate source selection, storage and endpoints. Five additional Firefox connections cover MiMo, Abacus, LongCat, Alibaba Token Plan and Qwen Cloud. Browser localStorage, remaining alternate sign-in strategies and live accounts are not implied by these implementations.

| Evidence | Result | Boundary |
|---|---|---|
| Independent Moonshot recheck | 78 observations passed | Synthetic regions, errors, aliases and scope |
| Independent Codebuff recheck | 63 local-auth and 13 optional-request observations passed | No real account |
| Independent GLM recheck | 126 observations passed | Synthetic trusted/untrusted source files and region binding |
| Independent Windows Copilot recheck | 53 observations passed | Includes a real synthetic gh child process, timeout/output/environment checks |
| Independent macOS Copilot recheck | 59 observations passed | Account/credential parsing, not installed app UI |
| macOS fresh full suite after Copilot corrections | 950 total, 941 passed, 9 skipped, 0 failures | 355 source/test/package hashes retained; no candidate installation |
| Windows r21 connected PC and QA CI | Core 772/772; build, package, WPF and CLI passed on connected x64; x64 and ARM64 CI passed | Moonshot settings capture in r21 uses preview quota; it is not balance rendering proof |
| Windows r23 connected PC | Core 780/780; 0 build warnings/errors; 56 GUI checkpoints, 66 PNGs; package and CLI passed | Actual connector → non-preview store → WPF captured four auth and five browser readings using injected fixtures; physical pointer remains inconclusive |
| Independent browser expansion recheck | 102 observations passed after region-race correction | Actual ProviderConnections + DashboardStore CN↔Intl transitions publish/save only the new region; this correction postdates r23 |

The r23 archive SHA-256 is 7e1e49cfe5027eaf1d57d416201d64897dce404cb2ea3dd95b24f3e967d47f4c (289 source files). It precedes the final Alibaba in-flight region freeze and the DeepSeek implementation. Those changes require subsequent native evidence. A build-only r22 failure (CA1861 in a fixture constant array) is retained; r22 was not executed natively. A DeepSeek test compile error used an incorrect model property name; it was corrected to Name without changing the intended assertion.

## DeepSeek, cooldown and Kimi continuation

- Windows r24 on the connected x64 PC: Core 816/816, six fresh command stages exited 0, build 0 warnings/errors, 57 GUI checkpoints and 68 PNGs. Two actual connector/store/WPF captures show DeepSeek Web 12.50 USD and API 42.00 USD with separate controls. The source ZIP (295 files) is SHA256 28793071c58eab158bda0221125ad91d9e24d68ac55637b1819af30dc0567ec7. Independent review verified all 125 delivered evidence files and pre/post source hashes. This is synthetic provider/DPAPI evidence, not a live DeepSeek account.
- r24 is not a final release candidate: independent review later found that its new cooldown scope included changing Vertex/Fireworks time query parameters and missed effective Azure legacy/Kiro bundled-profile selectors. Those defects were corrected after r24. Independent source-bound recheck now passes 130/130 observations, preserving required-versus-optional 429 behavior and keeping the original failed checks.
- Kimi's missing CLI/Web paths, Web subscription parsing and source controls are implemented. Its headline order now matches macOS's weekly-first behavior. Independent review reproduced and then verified fixes for malformed optional field types, quoted Cookie input and an HTTP transport ignoring cancellation. Auto/account lease review resolved HTTP400 fallback, API quote normalization and a damaged unselected browser jar blocking valid CLI fallback. The final independent recheck confirms no outstanding P1/P2 within that synthetic scope. r25 x64 QA CI (35604228430) passed Core873/873, build0 warnings/errors, 58 GUI checkpoints and70 PNGs; its two Kimi settings captures show CLI weekly33%/5h12% and Web weekly25%/5h65%/Total42%. Allegro is a store assertion, not visible plan-title proof in these captures. The connected x64 PC and native ARM64 CI subsequently passed the same873 Core tests,58 GUI groups and70 captures on frozen r25. Actual provider authentication was not exercised.
- Isolated macOS AppKit host: 41 actual C2 UI sources copied byte-for-byte; 127 host assertions with no assertion failure, four edges, three sizes, long quota/account scrolling, paging and injected hover/hit-testing. Own-window WindowServer captures (21 PNGs) were directly reviewed. The first host run recorded all results but needed termination for a host-only shutdown issue (exit -15); two later runs exited 0. Earlier cacheDisplay/ImageRenderer captures with white/missing scroll content were retained as capture failures, not used as visual proof. This does not verify physical mouse/wheel, screen reader, multiple monitors, normal native fade, Settings, actual account switching or whole-platform parity.

## Execution evidence

Evidence is retained locally under Artifacts/ParityCompletion-20260921 (ignored by Git). Every transferred Windows candidate has a source manifest and ZIP hash; native reports retain their candidate revision.

| Evidence | Actual result | Limit |
|---|---|---|
| Mac native edge/arrival tests | 7 passed | Native test windows, not a complete installed-app comparison |
| Mac full suite | 944 total, 935 passed, 9 skipped, 0 failed | Executed with an existing build; 355 source/test/package files were compared byte-for-byte with that build's source |
| Windows r2 on connected x64 PC | Core 430 passed; native smoke failed at missing sub-agent total | Failure preserved and fixed |
| Windows r3 | Core 430 passed; UI and CLI passed | Independent image review found chart baseline and narrow-header issues afterward |
| Windows r4 | Core 439 passed; UI and CLI passed, 52 captures | Chart/header fixes independently checked in actual captures |
| Windows r5 | Core 451 passed; UI, package and CLI passed | Actual pointer did not hit the notch |
| Windows r6 | Core 464 passed / 3 failed; build, package, UI and CLI passed | Three tests failed before exercising the reader because their byte snapshot opened a live SQLite DB without Windows sharing |
| Windows r7 on connected x64 PC | Core 467 passed, 0 skipped, 0 failed | Exactly one test helper changed from r6; all 249 other transferred files were verified unchanged, retaining the r6 native UI/package/CLI evidence |
| Windows r8 on connected x64 PC | Core 482 passed, 0 skipped, 0 failed; build, package, native UI and both CLI stages exited 0 | Includes Firefox/Kiro SQL safety and pending-read/pending-verification close guards; physical pointer remains intercepted by the lock screen |
| Windows ARM64 CI at cf0a41d | 481 passed / 1 failed; test process ran as ARM64 | Local-calendar test used fixed UTC timestamps and expected a different local day; reproduced under America/Los_Angeles, then corrected without changing production aggregation or expected totals. Native UI/CLI stages were not reached |
| Windows ARM64 CI at de6a754 | Core 482 passed, 0 skipped, 0 failed; native UI, CLI and isolated installer passed | Exact packaging archive SHA256 bb77013ddf57566fd231d814125d93fa75c1b205536c097f4e72955328d6f109; OS/process architecture both Arm64. This checkpoint precedes Groq console changes |
| Windows r9 on connected x64 PC | Core 513 passed; build, package, WPF and CLI passed | Groq source/settings/scope tests; later token-normalization corrections are recorded as r10 |
| Windows r10 on connected x64 PC | Core 515 passed, 0 skipped, 0 failed; build, package, WPF and CLI stages exited 0 | Includes final refreshed-JWT/empty-Session corrections; physical pointer remains lock-screen intercepted |
| Mac-hosted Groq Core checkpoint | Core 514 passed / 1 Windows-only test skipped, 0 failed | Cross-platform core execution, not WPF execution |
| NuGet audit | No known vulnerable package reported for all four projects, including transitive packages | Registry result at execution time, not a proof of absence of vulnerabilities |
| Mac-hosted Factory browser Core checkpoint | Core 551 passed / 1 Windows-only test skipped, 0 failed; solution build has no warnings or errors | Native WPF execution is recorded separately below |
| Windows r11 on connected x64 PC | Core 552 passed, 0 skipped, 0 failed; build, package, WPF and CLI passed | Factory browser transactions; ZIP SHA256 8ad35e3da0f10601a122c3661da2be6f6b8aaa2a01defc23a9a1f3538e024640, 257 source files. Groq/Factory connection screens captured; real pointer remains INCONCLUSIVE |
| Mac-hosted WorkOS/vault Core checkpoint | Core 589 passed / 1 Windows-only test skipped, 0 failed; solution build 0 warnings/errors | Synthetic request/atomic storage checks; actual Windows DPAPI execution below |
| Windows r12 on connected x64 PC | Core 590 passed, 0 skipped, 0 failed; all six build/package/GUI/CLI commands exited 0 | 263 source files; ZIP SHA256 60cdc2c14bc8cd1a37672bf819a1fdd6710547428a112ab998e01de433dadaeb. WorkOS refresh, DPAPI rotation and replacement/removal race assertions passed. GUI recorded 48 checkpoints, including physical pointer INCONCLUSIVE |
| Remote CI at 2896881 | macOS run 35579064769 and Windows run 35579064737 succeeded, including native ARM64 job | Factory browser checkpoint; WorkOS/vault changes are later and require their own CI |
| Mac-hosted Amp Core checkpoint | Core 659 passed / 1 Windows-only test skipped, 0 failed; solution build 0 warnings/errors | Parser/HTTP/source tests use synthetic responses; no real Amp account |
| Windows r13 on connected x64 PC | Core 660 passed, 0 skipped, 0 failed; six build/package/GUI/CLI commands exited 0 | 267 source files; ZIP SHA256 9949f498058008afd97abded6f4861faa102c75791ffda0543602347d5db9b1e. Native DPAPI/source-isolation/blank-setting/UI assertions passed, 49 checkpoints. Settings capture uses preview quota and does not prove a live Web reading; physical pointer remains INCONCLUSIVE |
| Native ARM64 CI at c4f9283 | Core 590 passed, 0 skipped, 0 failed; build, WPF, CLI and isolated CI installer passed | Job checkout 0ec6a54 has the same Git tree as c4f9283; 263 r12 sources bound. Archive SHA256 249080d31e70400535cda1433c814b766fca153d400bbe22a58b4b8c7d340db3. This precedes Amp Web; pointer hit WWAHost and remains INCONCLUSIVE |
| Windows r14/r15 on connected PC and r15 CI | Core 695 passed; native smoke failed | WMI 0x80041003 reproduced after helper readiness. Original error was initially masked by fixture cleanup; r15 preserved both |
| Windows r16 cross-build | Failed CA2201 | Diagnostic exception type corrected before the next validated build; this failed candidate is not a native PASS |
| Windows r17/r18 | WMI checks passed; native quota failed | r18 isolated Windows TLS reproduction identified fixture-only SChannel 0x8009030E for the ephemeral certificate key. Actual local usage remained unavailable |
| Windows r20 on connected x64 PC | Solution/package/GUI passed; Core 695 and CLI evidence reused after exact unchanged-input hashes | ZIP SHA256 f35ae54e61a569129d6cc8d2e1b3232b050b526e0e7919ce31775eec36f5ef1f; 274 files. WMI, PID ownership before TLS, 401-port fallback, connector→store→WPF, 33%/55% quota and stop invalidation passed. Graceful fixture exit 0; no forced kill |
| Windows r20 x64/ARM64 CI | Core 695/695 each; build/package/native WPF/CLI and isolated installer passed | [Run 35590282420](https://github.com/dlfkdLR/CodeRim/actions/runs/35590282420), QA head 7a346d85, all 274 ZIP sources bound. ARM64 extracted exact package with OS/process Arm64. Physical pointer remains INCONCLUSIVE |

The r6 pointer diagnostic observed successful cursor movement, but WindowFromPoint resolved to explorer's LockScreenBackstopFrame. Therefore real pointer hover remains INCONCLUSIVE until an unlocked interactive desktop is available. Routed-event checks are not counted as real pointer checks.

The installed Mac app was identified as version 2.1.5. Current source/test verification must not be represented as installation of the current candidate. Mac accessibility automation reached stale menu elements; this does not establish a product defect or a successful live navigation check.

## Reproduced problems and corrections

Paths below are relative to the repository. The location column names the function/component. Each row includes severity, reproduction, root cause/impact, correction, and verification.

| ID / severity | File and location | Reproduction and cause / impact | Correction | Verification |
|---|---|---|---|---|
| PAR-SEC-01 / Medium | Windows/src/CodeRim.Core/Services/BrowserCookies.cs, BrowserCookieJar | Cookie fixtures include suffix-lookalike domains, path-only cookies, expired/nonsecure records and header control characters; a flattened cookie string can cross its original request scope | Immutable bounded jar; exact host boundary, path, expiry and HTTPS checks on each request | BrowserCookieTests, BrowserCookieRequestTests and independent request fixtures |
| PAR-SEC-02 / Medium | Windows/src/CodeRim.Windows/Services/BrowserConnections.cs, CreateDialog | Close/cancel during import or reject new credentials; premature persistence could replace a working connection | Verify candidate reading before save, check closed state after awaits, retain old connection on failure | Native isolated DPAPI/import fixture and independent dialog lifecycle review |
| PAR-BUG-01 / Medium | Windows/src/CodeRim.Core/Resources/Plugins/qoder.js, request fallback | Only qoder.com.cn has a valid session; first region lacking credentials prevents the valid region being tried | Regional auth fallback; service errors remain service errors; cookies stay request-scoped | Synthetic regional auth/503/request-origin cases |
| PAR-BUG-02 / Medium | Windows/src/CodeRim.Core/Providers/ScriptProviders.cs, FetchAsync | Required HTML/empty 401 or optional activity 401; body parsing or global auth tracking gives wrong state | Required endpoint authentication tracked separately from optional activity | ScriptAuthBoundaryTests and independent HTTP fixtures |
| PAR-BUG-03 / Low | Windows/src/CodeRim.Core/Services/BrowserCookies.cs, parsing | A valid empty-valued cookie is discarded | Preserve empty values while rejecting malformed names/domains | Independent cookie recheck and regression fixture |
| PAR-BUG-04 / Medium | Windows/src/CodeRim.Windows/Views/UsagePane.cs, MetricSummary | Navigate to a direct sub-agent on r2; only input/output were visible and its total was absent | Visible total in session, model and selected bucket details | r3-r6 native navigation and analytics assertions |
| PAR-BUG-05 / Medium | Windows/src/CodeRim.Windows/Views/UsagePane.Timeline.cs, range/bucket navigation | Select a bucket, open a model, then Back; original selected range/details disappear | Preserve range and bucket selection across model navigation | Native selected-bucket/Back fixture |
| PAR-BUG-06 / Medium | Windows/src/CodeRim.Windows/Views/UsagePane.Timeline.cs, cost chart | Compare costs below one dollar; fixed dollar normalization obscures relative values | Normalize against actual maximum; keep unknown costs absent | Native 10:1 sub-dollar ratio and missing-cost tests |
| PAR-BUG-07 / Medium | Windows/src/CodeRim.Windows/App.xaml, button content alignment | r3 screenshots show bars floating above a common baseline | Bind VerticalContentAlignment; chart buttons stretch their content | Independent r4 pixel/bottom alignment inspection |
| PAR-BUG-08 / Medium | Windows/src/CodeRim.Windows/Views/UsagePane.Timeline.cs, header | At 360 DIP the range selector overlaps header text | Header moves to two rows below its content width threshold | Native 360/450/600 layout bounds and independent images |
| PAR-BUG-09 / Medium | Windows/src/CodeRim.Core/Services/ActivityReader.cs, snapshot scan | Rewrite bytes in a cached prefix or append to a replaced log; cached activity can survive incorrect content | File identity, frozen prefix hash, complete-line handling, bounded reverse scan and cancellation | ActivityReaderTests; independent append/rewrite/replace/partial-line probes |
| PAR-BUG-10 / Medium | Windows/src/CodeRim.Windows/Services/ClaudeSessions.cs, Read | Duplicate registry entries for one live session select an older process/start | Order by process/session start before update time | Native own-process registry fixtures in both orders |
| PAR-BUG-11 / Medium | Windows/src/CodeRim.Windows/ViewModels/DashboardStore.cs, UpdateSessionActivity | New Codex task stays busy but should have a new start; generic timestamp preservation keeps the previous task age | Preserve continuing time only for the same Claude process/state; accept Codex turn start | Native provider-specific entry-time fixture |
| PAR-BUG-12 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.cs, Scope / CanCache | A source with null account scope is invalidated on unrelated refreshes; CLI failure can retain an older account quota | Stable process/source marker for display lifetime; disallow snapshot restore and failure retention for unverified local sources | Independent Amp/production-store probes and native source UI tests |
| PAR-BUG-13 / Medium | Windows/src/CodeRim.Core/Services/WindsurfLocalUsage.cs, Parse | Legacy messages=100, remaining=70, flowActions=50, used=20 appeared as Daily/Weekly without a period contract | Messages/Flow actions labels and IDs, original count units, no invented reset | Independent exact-source legacy fixture and LocalStateDatabaseTests |
| PAR-SEC-03 / Medium | Windows/src/CodeRim.Core/Services/LocalStateDatabase.cs, Read / SqliteReadSafety | A 4KiB ItemTable view with recursive SQL executes beyond the busy timeout and can block synchronous credential scope reads | Ordinary stored table/column validation, VM work/time budget, row/SQL-size limits and exact key matching | Original view now rejected; generated/virtual schema rejected; 400k-row scan interrupted; normal WAL/encoding unchanged |
| PAR-SEC-04 / Medium | Windows/src/CodeRim.Core/Services/BrowserCookies.cs and KiroAuthentication.cs, external SQLite readers | Independent 4KiB recursive views exceeded a four-second watchdog; a busy timeout does not stop CPU-bound SQL, and valid-looking rows could allocate excessive data | Shared ordinary-table/stored-column verification, SQL VM/time/length budget, DB/WAL/SHM bounds, exact Kiro keys in one read transaction, Firefox per-value and cumulative limits | Original Firefox/Kiro repros now rejected in 46ms/1ms; 400k scans interrupted; generated/virtual tables and large values rejected; normal committed WAL and source bytes preserved; BrowserSqliteSafetyTests and KiroSqliteSafetyTests |
| PAR-BUG-14 / Medium | Windows/src/CodeRim.Windows/Services/BrowserConnections.cs, CreateDialog lifetime | Close while a profile read or provider verification is pending; a closed-state check alone leaves work running | Cancel both operations when the dialog closes, check closed after each await, dispose lifetime after completion, and handle safe-reader validation errors without replacing prior credentials | Independent lifecycle review; Connected-PC r8 NativeSmoke passed pending read/verification close and previous-credential preservation assertions |
| PAR-QA-01 / Low | Windows/tests/CodeRim.Core.Tests/LocalStateDatabaseTests.cs, SharedBytes | Windows r6 fails three encoding tests at File.ReadAllBytes before the reader is called; live writer requires sharing | Read fixture snapshots with ReadWrite/Delete sharing; retain byte-preservation assertions | Local full suite and connected-PC r7 467/467 passed |
| PAR-QA-02 / Low | .github/workflows/windows.yml, ARM64 archive verification | A PowerShell regex containing two backslashes does not split checksum whitespace | One-backslash whitespace regex | Independent source/synthetic review and de6a754 native ARM64 archive verification passed |
| PAR-QA-03 / Low | Windows/tests/CodeRim.Core.Tests/UsageScannerTests.cs, UsesLocalCalendarBoundariesAndExcludesFutureEvents | ARM64 CI and a local America/Los_Angeles run report zero Today tokens instead of 12; fixed 01:00Z data is the previous local day in western timezones | Build fixture timestamps and fixed now from local calendar instants; preserve Monday/Sunday, future exclusion and all expected totals/time assertions | Complete Core suite passes America/Los_Angeles, UTC and Asia/Seoul; de6a754 native ARM64 482/482 passed |
| PAR-BUG-15 / Medium | Windows/src/CodeRim.Core/Providers/GroqConsoleProvider.cs, FetchGroqConsole | Actual 75ms HttpClient refresh timeout exits after one request despite a valid direct JWT from the same profile | Treat request-local timeout as a refresh failure eligible for the same-profile fallback, while propagating the caller/deadline cancellation | Independent real HttpClient timeout resolves Ready with two requests; external cancellation stops at one; GroqConsoleTests and r10 core pass |
| PAR-BUG-16 / Medium | Windows/src/CodeRim.Core/Providers/GroqConsoleProvider.cs, GroqSession | Quoted Cookie header forms accepted by the pinned Swift normalizer result in NeedsAuth before any request | Strip balanced outer quotes before and after the Cookie prefix; preserve conflict and size checks | Independent pinned Swift comparison and single/double quoted input probes; r10 core pass |
| PAR-BUG-17 / Low | Windows/src/CodeRim.Core/Providers/GroqConsoleProvider.cs, token normalization and source selection | Refresh JWT with outer whitespace is rejected; empty Session marker is misrouted to enterprise metrics | Reuse bounded token normalization for refreshed JWT and classify an empty Session marker as missing console authentication | Outer CRLF trimmed, internal controls/oversize rejected before send, empty marker NeedsAuth with zero requests; independent 59-scenario recheck and r10 core pass |
| PAR-BUG-18 / Medium | Windows/src/CodeRim.Core/Providers/FactoryProvider.cs and NativeProviders.cs, Factory transaction | Rejected JWT sub=old-member survived a cookie-only retry, so a synthetic response attributed 91% to the prior user instead of the current user's 7% | Restart auth/me, billing and usage on every auth-mode change; only the accepted Bearer can supply a fallback identity | Independent 65 scenarios include later-stage 401/403/409 and current-user assertions; FactoryBrowserTests and r11 pass |
| PAR-BUG-19 / Medium | Windows/src/CodeRim.Core/Providers/FactoryProvider.cs, optional billing request | billing 429 was swallowed and legacy usage returned Ready after another request | Propagate the rate limit and retain backoff across subsequent fetches | Independent request traces stop at two calls; FactoryBrowserTests and r11 pass |
| PAR-BUG-20 / Medium | Windows/src/CodeRim.Core/Providers/FactoryAuthentication.cs, conflict recovery | A stale access-token cookie caused six failed requests even with a valid auth-session cookie | Bounded same-profile cookie variants after URI scoping, restarting the transaction without changing saved cookies | Independent stale-cookie, later-stage conflict, empty-cookie and bounded exhaustion checks; r11 pass |
| PAR-BUG-21 / Low | Windows/src/CodeRim.Core/Providers/FactoryAuthentication.cs and FactoryProvider.cs, input normalization | Quoted Authorization values and empty/padded profile IDs were rejected or miscompared | Normalize balanced header quotes and trim all profile/sub/returned IDs; reject IDs over 256 characters or containing internal controls | Independent pinned-source comparison and field boundary checks; r11 pass |
| PAR-BUG-22 / Medium | Windows/src/CodeRim.Core/Providers/FactoryWorkOsProvider.cs, response validation | Synthetic refresh response has non-string or duplicate access/refresh/organization fields; permissive field access silently reused old values and accepted ambiguous rotation | Require one correctly typed canonical field, preserve omitted/null optional values, reject conflicting identity before save or quota | Independent 56-scenario recheck all passed; FactoryWorkOsTests and connected-PC r12 |
| PAR-BUG-23 / Medium | Windows/src/CodeRim.Core/Services/AtomicCredentialFiles.cs and Windows/src/CodeRim.Windows/Services/CredentialVault.cs, commit/refresh coordination | Concurrent fixed-name staging writes, read/delete races and delayed refresh reproduce wrong final value, failed read or stale-account overwrite in isolated storage probes | Unique temporary files, cross-process commit lock and versioned compare-and-swap; separate asynchronous refresh lease lets manual save/delete proceed | Independent concurrent/process probes and Windows r12 DPAPI rotation/replacement/delete/same-value reconnect/32-write checks. Old UI-only writer reachability was not established; new background refresh requires this protection |
| PAR-BUG-24 / Medium | Windows/src/CodeRim.Core/Providers/AmpBrowserUsage.cs, candidate HTML/parser context | Candidate reads script examples in HTML comments, raw-text/inert elements, non-JS script types, strings, expressions or uncalled functions as valid 0% quota | Parse only active inline JS and the two pinned top-level Svelte hydration literal shapes; no downloaded JS execution, duplicate/spread/ambiguous objects rejected | Independent initial/second/final probes preserved, AmpBrowserTests and r13. Current live server layouts beyond those fixtures are unverified |
| PAR-BUG-25 / Medium | Windows/src/CodeRim.Core/Providers/AmpBrowserProvider.cs, redirect classification | Candidate treated canonical settings redirects as auth failure and missed pinned auth-domain/sign-in aliases | At most three same-origin HTTPS settings redirects with per-URI cookie scope; known login returns NeedsAuth without follow; untrusted targets remain Error | Independent redirect/cookie/cancellation traces and ten final login/unsafe-URL probes; Core and r13 |
| PAR-BUG-26 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.cs and DashboardWindow.cs, effective settings | Save blank executable path then change environment A to B: actual CLI quota changes 37% to 89% while old scope stays; a legacy blank source plus environment CLI incorrectly restores cache | Shared trimmed blank-to-environment resolver for native fetch, scope, cache policy and Amp/Windsurf source UI | Independent 50 scenarios passed after correction; r13 native DPAPI/source controls. Current source picker does not itself create a blank source |
| PAR-BUG-27 / Medium | Windows/src/CodeRim.Core/Providers/AntigravityLocalUsage.cs, Cadence | Pinned bucket aliases such as gemini-5h and weekly lost their reset-window duration | Preserve supported bucket/label aliases and family suffixes | Independent protocol recheck plus native r20 300/10080-minute assertions |
| PAR-BUG-28 / Medium | Windows/src/CodeRim.Core/Providers/AntigravityLocalUsage.cs, TryRead | Summary HTTP/parse/network failure or full deadline consumption prevented supported legacy fallbacks | Bounded per-stage time budget; recover only same-process transient/shape failures; auth/throttle remain distinct | Independent initial failures and 30 added recheck cases; Core fallback/deadline tests |
| PAR-BUG-29 / Low | Windows/src/CodeRim.Core/Providers/AntigravityLocalUsage.cs, Plan | Successful quota omitted plan metadata returned by user status | Safe bounded optional plan enrichment | Core and independent fixtures; native r20 Pro |
| PAR-BUG-30 / Medium | Windows/src/CodeRim.Windows/Services/AntigravityLocalConnection.cs, port selection | First owned listener returned 401 and hid a later valid quota listener | Continue across bounded same-process owned ports; retain auth result only if none succeeds | Native r20 deniedRequests=1 then quotaRequests=2 on PC and both CI architectures |
| PAR-BUG-31 / Medium | Windows/src/CodeRim.Windows/Services/WmiProcessCommandLine.cs, proxy security | Actual Windows PC and CI returned WBEM_E_ACCESS_DENIED because the blanket targeted generic IUnknown rather than the queried proxy | Typed IWbemServices/IEnumWbemClassObject proxy security and sanitized operation/HRESULT diagnostics | r15 original failures preserved; r17/r20 WMI identity and CSRF parsing passed |
| PAR-TEST-01 / Medium | Windows/src/CodeRim.Windows/Views/NativeSmoke.Antigravity.cs, TLS fixture/lifetime | Windows SChannel rejected the test's ephemeral private key; cleanup could mask the failure; forced kill could skip disposal | Disposable non-persistent user-key association, zero exported bytes, graceful stop with exit-code gate, separate failure/shutdown/cleanup records | Windows .NET10 minimal reproduction and independent forced-kill fixture; r20 graceful exit 0 on x64/ARM64. Key-container absence was not separately measured |
| PAR-PERF-01 / Medium | Windows/src/CodeRim.Core/Providers/AmpBrowserUsage.cs, raw-text scanning | A 1.71M-character candidate fixture with 10,000 empty textarea elements repeatedly copies the remaining HTML: about 2.99 seconds and 30,622 MiB cumulative allocation | Search the original string by offset and enforce deadline/cancellation during HTML traversal | Same independent fixture about 24 ms and 8.0 MiB cumulative allocation; bounded-allocation regression and r13. These are cumulative allocations, not peak resident memory |

## Additional reproduced defects

| ID / severity | File / location | Reproduction and cause | Correction and verification |
|---|---|---|---|
| PAR-BUG-32 / Medium | Sources/CodeRim/Notch/CopilotNotchProvider.swift, credential selection | Blank GH_TOKEN hides GITHUB_TOKEN; a token can borrow an unrelated GitHub CLI account label; malformed indentation selects an inactive account | Shared first-nonblank and bounded active-account semantics; independent 59 cases, 14 focused tests and fresh full Swift suite |
| PAR-BUG-33 / Medium | Windows/src/CodeRim.Core/Services/GitHubAuthentication.cs, hosts parser | Conflicting/incorrectly indented hosts YAML can choose an inactive token | Reject ambiguous structure and use bounded gh fallback; independent 53-case recheck |
| PAR-BUG-34 / Medium | Windows/src/CodeRim.Core/Services/CodebuffAuthentication.cs and Providers/NativeProviders.cs | Wrongly typed local credential fields accepted; optional subscription 429 throttles the next required usage request | Strict field types and required-versus-optional retry policy; independent 63 + 13 cases, r21 native tests |
| PAR-BUG-35 / Medium | Windows/src/CodeRim.Core/Services/GlmAuthentication.cs, explicit key precedence | Invalid explicit key silently falls through to another local account | Nonblank explicit input stays authoritative; trusted host/region matching and 126-case recheck |
| PAR-BUG-36 / Medium | Windows/src/CodeRim.Core/Providers/TokenPlanProviders.cs, SEC fallback | Imported host/path-scoped dashboard cookie is absent from the gateway Cookie header and its SEC value is never resolved | Read only the named SEC value for the paired fixed gateway; preserve Cookie/CSRF URI scope; browser regression and 102 independent cases |
| PAR-BUG-37 / Medium | Windows/src/CodeRim.Core/Providers/NativeProviders.cs, region transaction | Change Alibaba region while dashboard request awaits: fixed URL receives new-region Origin/form values | Freeze the region for one transaction; two directions tested; actual store discards old 33% and emits/persists new 67% only |
| PAR-BUG-38 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.cs and Core/Providers/DeepSeekAuthentication.cs | Platform-token environment alias is sent as an API key to the balance endpoint | Typed API/Web resolution, separate encrypted slots, fixed endpoints and source/account invalidation; Core regression checks |
| PAR-BUG-39 / Medium | Windows/src/CodeRim.Core/Providers/DeepSeekBalance.cs, availability flag | is_available=false with nonzero money is displayed as Available balance | Preserve amount as Total balance with unavailable-for-calls message; dedicated regression test |
| PAR-BUG-40 / Medium | Windows/src/CodeRim.Core/Providers/DeepSeekAuthentication.cs, Resolve | Unused encrypted slot throws while selected API/Web slot is valid | Resolve only the selected slot; Auto does not read Web when API exists; three damaged-slot regressions |

## Further reproduced defects and corrections

| ID / severity | File / location | Reproduction / cause | Correction / evidence |
|---|---|---|---|
| PAR-BUG-41 / Medium | Windows/src/CodeRim.Core/Providers/HttpProviders.cs and NativeProviders.cs, retryAfter | Account A gets 429; switching to B or another source still blocks without a request because only provider ID is keyed | Hash selected credential, stable destination and effective selectors. Remove only known volatile query timestamps; include Azure deployment alias and Kiro bundled profile. Independent 130 observations passed; original and intermediate regressions retained |
| PAR-BUG-42 / Medium | Windows/src/CodeRim.Core/Providers/CodingPlanProviders.cs, ParseKimi | Weekly25/session10 renders10 as main while actual macOS adapter renders25 | Weekly-first selection; retain modern-pool precedence/known zero assertions, matching pinned snapshot and actual ExtendedNotchProvider flow |
| PAR-BUG-43 / Medium | Windows/src/CodeRim.Core/Providers/KimiConnection.cs, optional stats | Numeric feature, object type or string enabled is treated as an absent optional value and displays invalid quota | Validate known field types; preserve primary quota and mark missing optional stats Partial |
| PAR-BUG-44 / Medium | Windows/src/CodeRim.Core/Providers/KimiConnection.cs, optional join | A handler ignores cancellation: two-second grace waits beyond three seconds, and caller cancellation also waits | Bound join and HTTP wait independently, dispose late HTTP responses, preserve completed optional values; independent repro improved from about3004ms to2004ms and caller cancel from3002ms to immediate |
| PAR-BUG-45 / Low | Windows/src/CodeRim.Core/Services/KimiAuthentication.cs, WebToken | Quoted cookie after Cookie: prefix is rejected although pinned normalizer supports it | Normalize quotes after the prefix; both quote forms independently retested |

Kimi test setup failures were also retained separately: an escaped string failed compilation; a valid CLI fixture under macOS's symlinked /var path was rejected by the guarded reader. The test now uses the physical /private/var path. Production link/reparse rejection was preserved.

## Remaining requirements

The original 50-item matrix is retained as the baseline; additional tests do not automatically change its completion percentage. In particular, implementing one authentication source is not equivalent to validating every source or all 70 live accounts.

| Requirement | Current evidence and remaining work |
|---|---|
| APP-01 | Windows synthetic native routing and reopen behavior verified; current installed Mac full navigation still needs direct confirmation |
| PROV-02 | All 70 IDs have implementations; live credentials/account permissions and actual quotas are not verified for every provider |
| PROV-03 | Expanded native/script success and error contracts pass synthetic tests; all current live response shapes remain unverified |
| ACCT-03 | Native Windows DPAPI fixtures pass; actual saved-account switching and real Keychain transitions remain separate |
| ACCT-04 | Twenty Firefox cookie import paths implemented and verified with synthetic profiles; other browser engines/localStorage strategies remain |
| ACCT-05 | Amp API/CLI/Web, Windsurf cache, Antigravity OAuth/Local IDE and Groq console/Stytch plus Factory browser and saved WorkOS refresh profiles added; automatic browser localStorage remains. Native Antigravity synthetic execution passed on connected x64 and CI x64/ARM64 |
| UI-02 | Windows in-place plan/status refresh and Usage refresh verified; complete Mac live-click comparison remains |
| UI-04 | Monitor-limited cards and layout checks pass; physical wheel traversal of long cards remains |
| UI-06 | Native fade/arrival recheck passes all seven tests; complete user-desktop transition comparison remains separate |
| UI-07 | Known total, cost-baseline and narrow-header regressions corrected; complete pixel/interaction parity is not established |
| UI-08 | Synthetic geometry and accessibility properties checked; physical mixed-DPI/multimonitor and screen-reader workflows remain |
| ACT-02 | State/focus guards and native fixtures checked; actual notification, sound and terminal activation workflows remain |
| ACT-03 | Requirement correction: actual macOS NotchController registers activity only for Codex/Claude, matching Windows. Additional provider activity is not a missing current-macOS feature; quota reader presence does not imply an activity source |
| REL-03 | No eligible code-signing certificate was found in the Windows user's or machine's store; signed installer/in-place update remains unimplemented |
| REL-04 | Connected x64 native execution verified; de6a754 exact ARM64 archive, native UI/CLI and isolated CI installer passed. Subsequent authentication changes need their own ARM64 CI; production installation/update remain separate |

No real tokens, passwords, certificate subjects/private keys, or user database contents are included in this record.

## Harness result

The development-harness candidate verification reports FAIL/EVIDENCE_INVALID because its fixed verification inputs include the newly added tests and changed Windows workflow (and generated build files). Its plan also has no native Swift/.NET/security adapters. This is not recorded as ACCEPT or silently replaced by native results. The sandboxed worker could not import outside-workspace evidence files, so the original native/independent artifacts remain at their original paths. Direct native builds/tests and connected-PC execution were outside that harness boundary; all product edits were made through the worker. The earlier independent packet review found the Firefox SQL defect; that failure is preserved. The subsequent shared-reader review reproduced the old Firefox/Kiro problem, verified the corrections and found no remaining confirmed P1/P2 within that change. Packet/gate records remain distinct from product test results.

## StepFun authentication continuation

The original Windows connector returned NeedsAuth with zero requests for both environment and saved username/password inputs. A manual401 stopped without RefreshToken, and a200 token-expired payload became Error. The independent baseline retains8 scenarios/35 observations and10 pinned Swift parsing checks.

The candidate now connects password login, device registration, bounded refresh/retry, Auto/Manual controls and Firefox URI scopes. Encrypted manual-token rotation preserves an owner identity and uses CAS so stale refresh cannot recreate a removed account. The first local Core run passed895 tests with one Windows-only skip; later corrections are verified by frozen r27. Connected Windows x64 and x64/native ARM64 CI each pass903/903 tests, build with zero warnings/errors and complete58 GUI groups with72 PNGs. Manual has no Firefox import control, and Auto tests actual dialog verification, DPAPI save and ordinary refresh. The prior r26 CI success did not catch the Manual import bug; the original failed fixture remains documented.

| ID / severity | File / location | Reproduction and cause | Correction / validation |
|---|---|---|---|
| PAR-BUG-46 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.Kimi.cs, Auto fallback | API or intermediate CLI HTTP400 switches accounts despite an invalid request | Typed transport result prohibits fallback on400; independent original failure and both corrected boundaries retained |
| PAR-BUG-47 / Low | Windows/src/CodeRim.Core/Services/KimiAuthentication.cs, ApiKey | Balanced outer quotes are forwarded or reject a valid API key | Normalize only API input; CLI token checks unchanged; four Core inputs plus independent quote fixtures |
| PAR-BUG-48 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.Kimi.cs, KimiCandidates | Unselected browser jar { / null / {} throws before the valid CLI can be tried | Isolate malformed candidate data; three direct connector rechecks and native regression assertions |
| PAR-BUG-49 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.StepFun.cs and Core/Providers/StepFunConnection.cs | Username/password has no request path; expired manual login never recovers | Fixed-origin device registration/login and bounded token refresh with separate manual policy;24 initial StepFun Core cases plus later regression; r27 native connector/store/WPF verified |

The first StepFun build exposed a helper-name error and analyzer violations; these were corrected before the successful Core run and solution build. The original logs are retained. No live provider token, password or production browser profile was used by these fixtures.

## StepFun and MiniMax follow-up verification

Frozen r27 is SHA256 8d46d98770b951b259c95c6cc514e8883ca989a75097d17bc4f250f7b779fc44 (306 files). CI run35609530049 on QA head5d5f1a308083bbeec8dd729168b0430d8c7c9b53 verifies x64 and native ARM64; the connected PC independently runs seven fresh command stages. Both CI architectures and the PC pass903/903 Core tests. The native fixture is isolated and uses synthetic credentials. Locked-desktop physical hover remains INCONCLUSIVE.

The current Mac StepFun adapter is independently verified in32 boundary cases. Final full Swift execution has962 tests:953 passed,9 skipped,0 failed. The skips remain skips. Auto tries cached token, distinct environment token, then password; token-only and cache-only responses use the actor-isolated revision guard before persisting. Manual/Off/source restrictions retain upstream behavior. Copilot and StepFun source changes in the private candidate are not proof of installed-app behavior.

| ID / Severity | Files / location | Reproduction and cause | Correction and verification |
| --- | --- | --- | --- |
| PAR-BUG-50 / Medium | Windows/Views/DashboardWindow.cs, AddSecretField; Mac ExtendedProviderConfiguration.swift | Password spaces were removed; eager typed password selection could precede an environment token | Preserve raw StepFun password and source order; PasswordBox save assertion, Swift tests and independent traces |
| PAR-BUG-51 / Medium | Sources/CodeRim/Providers/StepFunPasswordRecovery.swift and ExtendedNotchProvider.swift | Masked refresh error caused password fallback after429/503; late responses could restore a removed account; no-pair paths bypassed guard | Direct auth stages, typed auth-only fallback, ordered candidates and MainActor revision-checked cache commit; independent32/32 and final Swift suite |
| PAR-BUG-52 / Medium | Windows/Views/DashboardWindow.cs and NativeSmoke.StepFun.cs | r26 Manual Firefox import saved a jar ignored by normal refresh; original smoke omitted that final fetch | Auto-only control; actual dialog→DPAPI→ordinary fetch assertion; r27 Manual PNG inspected and x64/ARM64/PC pass |
| PAR-BUG-53 / Medium | Windows/Core/Providers/SubscriptionProviders.cs | Video-first arrays and Text_Generation/TextGeneration/whitespace Weekly services selected70% instead of text4% | Stable text interval→weekly→other ordering; independent30/30 in both regions and r27 regression |
| PAR-SEC-05 / Medium | Windows/Core/Providers/MiniMaxWeb.cs, URI request stage | One Firefox jar with platform accountA and www accountB combined A quota with B metadata or remains | Check selected group/session at every URI; reject conflict before HTTP. Original repro retained; independent boundary recheck passes; frozen r29 native MiniMax assertions and captures passed before a later PC fixture cleanup failure |
| PAR-BUG-54 / Medium | Windows/Core/Providers/MiniMaxWeb.cs, HTML→remains stage | Structured HTML quota followed by unnecessary remains;401 discarded valid quota | Only missing/coarse quota invokes remains; independent traces and Core regressions |
| PAR-BUG-55 / Medium | Windows/Core/Providers/MiniMaxWeb.cs, Firefox auth candidates | Imported HERTZ was never tried as Bearer though same copied capture succeeded | Same-session Bearer then one cookie-only auth/parse retry; no429/server/network fallback; independent retry cases pass |

MiniMax Web adds region/source controls, encrypted independent API/Web/browser slots, copied-header/cURL parsing, HTML/JSON quota, current subscription metadata, bounded billing and stale-selection rejection. Frozen r29 CI runs Core966/966 and61 GUI groups/75 PNGs on x64 and native ARM64. The connected PC also passes Core966/966 and the MiniMax API/Global/China assertions and three captures, then fails a later GUI cleanup step after51 groups/72 PNGs. This partial PC run is preserved as FAIL; it is not a complete native release verification.

A harness worker overlapping SwiftPM initially reported INCONCLUSIVE when generated .build XCFramework symlinks appeared during isolation scanning. Source work was preserved, the scratch build moved outside the candidate and affected checks rerun. The formal C2 gate remains pending; C1's REJECT is retained separately.

The parser-only MiniMax follow-up found upstream edge cases: plan-name-only may become0%, metadata may select an unsold catalog plan, and billing has no currency model. Windows requires measured quota, explicit current subscription objects and does not infer a billing currency. Initial compile/analyzer failures and two minutes/days regex regression failures were corrected; original failed logs are retained.

## MiniMax parser corrections after frozen r28

The frozen r28 Windows run is retained as a successful native UI checkpoint with known parser defects. x64 CI Core952/952 and native61 groups/75 PNGs passed, including actual MiniMax API80%, Global Web4% and China Web8% settings captures. This fixture supplied named quota and empty billing, so it did not cover the independently reproduced errors below. r28 is not release-ready.

Independent parser execution reproduced51 cases:43 passed and8 failed, corresponding to5 defect paths. After the corrections,78/78 cases pass, including additional raw-text/template/MIME/duplicate-script/size, date, numerical, canonical-page and metadata-scope boundaries. The final source differs from the executed parser copy only by removal of an unused private helper; that exact diff has a separate binding receipt. Authentication's102-case evidence covers its own source revision. Later direct-Core source normalization/region-freeze changes have5 additional Core regressions.

| ID / severity | File / location | Reproduction / impact | Correction / verification |
| --- | --- | --- | --- |
| PAR-BUG-56 / Medium | Windows/src/CodeRim.Core/Providers/MiniMaxWeb.cs, MiniMaxBillingSuccess | Empty result plus FAILED status counted999 failed tokens together with5 successful tokens | Blank result falls back to status; original1004 becomes5; Core and independent fixtures |
| PAR-BUG-57 / Medium | Same file, ParseMiniMaxWebPlan | Valid single NEXT quota without model_name was rejected instead of showing75% | Web-only single-service compatibility; API parser contract preserved; pinned fixture and Core regression |
| PAR-BUG-58 / Medium | Same file, MiniMaxHtml | Comment/template NEXT examples70% overrode active quota4% | Bounded HTML tag walk ignores inert/raw-text content and accepts one inline NEXT JSON script; nested/template/MIME/source/duplicate boundary tests |
| PAR-BUG-59 / Medium | Same file, MiniMaxPageIdentity | Same3-token page changed whitespace/property order and was counted twice | Canonical JSON page identity, bounded retries and Partial history; semantic number/Unicode forms tested |
| PAR-BUG-60 / Medium | Same file, metadata selection | packages[0].current_subscribe was treated as the purchased plan | Only one direct root/data current-subscription object may supply metadata; catalog and conflicting-object tests |
| PAR-BUG-61 / Medium | Windows/src/CodeRim.Core/Providers/NativeProviders.cs, MiniMax entry | Mixed-case Auto/Web was not normalized, and API region could vary between fallback calls | Normalize/validate source once and freeze region for the complete request; five direct-Core regressions |

An independent parser follow-up accidentally overwrote a prior report path and four synthetic fixtures. All four fixtures were restored from byte-identical backups and the original225-file manifest matches. The original c7b995… JSON report could not be recovered; its Markdown, source hashes and execution outputs were retained. New parser evidence and a recovery/loss receipt are stored separately and do not impersonate that original report.


## Alibaba CLI, MiMo session recovery and concurrency continuation

The next candidate adds Alibaba Auto/CLI/Web and selected-region controls, bounded Bailian CLI execution, and MiMo recovery from the selected Firefox profile's sessionstore files. Browser imports commit only if the saved connection version still matches the version captured when the dialog opened. The credential store now queues in-process writers before starting its cross-process lock deadline.

Local Core execution after these changes has1029 tests:1023 passed,6 Windows-only tests skipped,0 failed. The solution cross-build has0 warnings/errors. A subsequent Alibaba optional-Web catch initially lacked System.IO and failed with two compiler errors; the corrected full solution build again has0 warnings/errors. NuGet's current advisory source reports no known vulnerable direct or transitive packages in all four projects. None of these Mac-hosted checks executes Windows Job Objects, DPAPI, native WPF or actual provider accounts.

| ID / severity | File / location | Reproduction / cause and impact | Correction / verification |
| --- | --- | --- | --- |
| PAR-BUG-62 / Medium | Windows/src/CodeRim.Core/Services/AtomicCredentialFiles.cs, Acquire | Published Windows CI fails under concurrent local saves; independent48-thread load reproduces timeouts while each individual file-lock hold is short. Local writers consume each other's entire2-second cross-process polling budget | Ref-counted per-root in-process semaphore before the file-lock timer;1440 independent writes/reads complete with0 errors, cross-process CAS keeps one winner, external lock timeout and later recovery remain tested |
| PAR-SEC-06 / Medium | Windows/src/CodeRim.Windows/Services/BrowserConnections.cs, CreateDialog; CredentialVault.cs, SaveIfUnchanged | Finish a verified import after another window creates/replaces/removes its saved connection: unconditional save can overwrite the newer account | Capture opaque encrypted-entry version, compare-and-swap even for an initially absent slot, and show a conflict instead of saving. Independent nullable-CAS checks pass; three actual DPAPI/dialog race fixtures pass on the connected PC in frozen r30. Content-version CAS does not detect an absent→created→deleted ABA cycle |
| PAR-BUG-63 / Medium | Windows/src/CodeRim.Windows/Services/ProviderConnections.Alibaba.cs, AlibabaSource | A legacy Web connection defaults to Web only while credentials exist; deleting them silently chooses CLI on the next refresh | Persist the initial Web/Auto choice. Independent migration cases preserve Web after removal |
| PAR-BUG-64 / Medium | Same file, AlibabaSource version binding | A user saves CLI between a missing-value read and a separate version read; migration then uses that newer version to overwrite CLI with Web | Read value and version together and CAS against that snapshot; independent migration scenarios preserve explicit changes before/after the snapshot |
| PAR-BUG-65 / Medium | Same file, ResolveAlibaba | Explicit Auto with valid CLI plus malformed/decryption-failing optional Web state returns Error before running CLI | Isolate the optional Web bundle only in Auto. A failure drops browser/cookie/sec-token together, never borrowing another manual account; explicit Web remains fail-closed. Native regression exercises three malformed jars with both valid and failed CLI |
| PAR-BUG-66 / Medium | Windows/src/CodeRim.Core/Services/BoundedProcess.cs and WindowsIsolatedProcess.cs | The CLI parent exits while a descendant keeps stdout open; parent-based process-tree kill cannot terminate the orphan | Create the process in a kill-on-close Job Object before its first instruction. Actual native fixtures cover early parent exit, waiting parent, output limits and argument/environment transfer; Mac skips are not native PASS |
| PAR-BUG-67 / Medium | Same files, standard handles and finally cleanup | A simultaneous framework process launch may inherit an isolated child's pipe writer, making uncancellable reader cleanup outlive the deadline even after the Job closes | Serialize product process creation and use cancellable overlapped pipes. An independent Windows before/after PE fixture reproduces a blocked reader at the 6.5-second observation and confirms fixed cancellation before release; the r31 replacement regression still requires native execution |
| PAR-BUG-68 / Medium | Windows/src/CodeRim.Core/Services/MiMoFirefoxSessionImport.cs, candidate parsing | Malformed LZ4/JSON schema can stop valid recovery, and fractional positive expiry can truncate to the session-cookie sentinel | Distinguish malformed from valid empty state, validate expiry before integer conversion and retain whole-session pairing. Independent initial70-case failures were corrected and rechecked |
| PAR-BUG-69 / Medium | Same file, ReadStable and resource limits | An oversized header or file changing during read falls through to an older session, potentially reviving a stale account | Typed resource/unstable failures stop recovery fallback and retain the database candidate. Independent final14 checks include three file-mutation reproductions and header limits; the r30 connected-PC profile/sessionstore/dialog/DPAPI/ordinary-store fixture passes; real MiMo accounts remain unverified |
| PAR-TEST-02 / Medium | Windows/src/CodeRim.Windows/Views/NativeSmoke.cs, pre-hover popup cleanup | r29 PC GUI creates KeyEventArgs from an already-closed popup whose retained child has no PresentationSource; inputSource=null aborts the remaining groups | Send cleanup Escape through the visible notch window, then assert the popup closed. The later account-popup Escape assertion is retained. r29 failure/log/image remain preserved; frozen r30 PC completes all62 groups/77 PNGs |

MiMo import requires a complete service-token/user-ID pair in one selected-profile session candidate and sends it only to the pinned MiMo HTTPS endpoint. Empty current state does not resurrect an older recovery file. The DB/session candidates are not merged across accounts, containers or profiles. The native fixture now creates an empty SQLite cookie DB plus compressed Firefox recovery data and exercises profile discovery, dialog verification, encrypted save, ordinary refresh and WPF.

Alibaba CLI uses a fixed read-only command, four region mappings, explicit executable selection, a restricted environment and bounded UTF-8 output. Web Team selection does not redefine personal CLI rolling quota. Auto/CLI readings do not use a persisted accountless cache. Synthetic CLI injection in the GUI fixture proves connector/UI routing; it does not prove a real Bailian sign-in. The r30 native process fixtures execute PowerShell helpers. The r31 revision uses a test-only .NET PE helper with startup, process identity and normal-exit acknowledgements; neither makes a provider request.

The independent Alibaba review's malformed ad hoc build was mistakenly followed by dotnet execution without a DLL-existence guard. The SDK first-use output reported installation of an HTTPS development certificate in that isolated run. Actual Keychain mutation was not verified; the logs are preserved, no certificate trust change or cleanup was performed, and subsequent SDK commands disable development-certificate generation and require successful build/DLL checks. This environment incident is separate from product authentication.

The frozen r29 archive is SHA256 c668b89304c2931d382885000e118facd7c985cfc8cbc3844e995a8bbead5ee2 (311 source files), QA head7d91aa2f13f2f869d28642d2c87ccb5d83f770a4, CI run35613348043. It predates all changes in this section. Mac authentication commit b9a4cde20f4d677e2dc81b3e0d8c1915efb89cdc is pushed and its Mac CI35611787222 succeeds; the installed Mac app remains a separate state. No new release tag or candidate installation is established by this checkpoint.


## DeepSeek details and native process regression continuation

The connected Windows PC completed frozen r30 (320 source files, SHA256 a14b8858efe4e1b8eaa99b4c0bd40daf36675ce4390aa3434328f9c11add6b0d): seven fresh stages, Core1029/1029 with0 skips, solution0 warnings/errors,62 GUI groups/77 PNGs and CLI checks. All158 returned evidence files and the source hashes before/after execution were independently verified. Five standalone process cases also passed on that PC. No real provider account or installed-app replacement is established by these fixtures.

The same r30 source at QA head452e6f5ca19375bf574b32d2f2d100068c1e3f51 failed CI35618764490: Core1026 passed/3 failed/0 skipped. The oversized-output case reached its8-second timeout; two descendant cases reached3 seconds without child.pid. WPF/package/GUI/ARM64 stages were skipped after that failure. The logs do not establish whether interpreter startup or another resource constraint caused it. This CI failure is retained and r30 is not release-ready.

A separate native Windows experiment builds and runs pure .NET PE fixtures against both baseline and fixed product sources. The baseline's managed and raw foreign-launch cases still wait at a6.5-second observation after a5-second cancellation deadline, until the fixture releases the inherited writer. The fixed managed case serializes creation and returns0 before that release; the fixed raw foreign launch can inherit the three handles but cancellation returns before release. Exact return latency was not measured. All62 returned files and9 fixed inputs were verified; fixture processes were absent afterward. The fixed product files match r30. This experiment is separate from the replacement r31 test suite.

DeepSeek now has an explicit, initially disabled Detailed Web usage control. It augments a valid balance with Today, Last30 days (or UTC current month fallback), Requests, API keys and Top model, grouped as Usage in both the notch popover and dashboard. It never sends API-key mode credentials to the Web detail endpoints, never invents percentages, and freezes the selected source/setting for the request. Optional failures preserve balance as Partial and remove stale detail rows. Only fixed provider HTTPS endpoints are used, with a shared5-second detail deadline, bounded JSON and bounded arrays.

| ID / severity | File / location | Reproduction / cause and impact | Correction / verification |
| --- | --- | --- | --- |
| PAR-BUG-70 / Medium | Windows/src/CodeRim.Core/Providers/DeepSeekUsageDetails.cs, numeric parsing | Very small or overprecise numeric strings round through decimal parsing, reporting zero or altered usage | Require exact decimal representability, nonnegative costs and integral bounded counts; independent malformed, underflow and precision scenarios |
| PAR-BUG-71 / Medium | DeepSeekDetailsConnection.cs, paired detail requests; DeepSeekUsageDetails.cs, authentication envelopes | JSON authentication denial in one request is masked by invalid JSON/503/network failure in the other, allowing monthly fallback | Inspect completed auth envelopes before transport/parse fallback; directly inspect duplicate root code/data/biz_code positions before structural rejection; mixed-failure regression cases |
| PAR-BUG-72 / Medium | DeepSeekUsageDetails.cs, aggregation | Duplicate category/bucket/day fields, malformed unselected currency blocks, anonymous-key collision or local month boundaries can miscount usage | Validate all relevant structures, normalize category identity, keep anonymous identity typed and use UTC month boundaries; independent parser fixtures and Mac mapping comparison |
| PAR-TEST-03 / Medium | Windows/tests/CodeRim.Core.Tests/WindowsIsolatedProcessTests.cs and tests/CodeRim.ProcessFixture | r30 CI fails before three PowerShell fixtures produce the intended observations, so timeout alone cannot prove the child ran | Replace interpreter commands with a PE helper; verify child PID/start time/nonce and startup acknowledgements, observe the early parent's exit0, add detached-output early exit, preserve3/8-second bounds and the raw foreign-launch race |
| PAR-TEST-04 / Medium | WindowsIsolatedProcessTests.cs, failure cleanup | Empty or partial child.pid throws during cleanup and skips foreign sibling termination/Dispose; independent3/3 reproduction | Read at most129 characters, validate identity without throwing, and run foreign cleanup in nested finally with bounded noncancelled wait; three malformed-identity regressions |
| PAR-BUILD-01 / Low | Windows/CodeRim.Windows.sln, ProcessFixture configuration | A helper absent from the solution configuration builds as Debug while the Release copy target can find stale output | Add explicit Debug/Release solution mapping and copy exact apphost/DLL/runtimeconfig/deps outputs; independent clean Debug and Release builds verify all four hashes |

These additions remain source/fixture implementations until the frozen r31 PC and CI executions establish native results. The Mac-hosted solution build is0 warnings/errors; Mac-hosted Core skips Windows-only execution. Kimi Desktop, Alibaba Coding Plan Web, browser localStorage discovery, trusted Windows signing/in-place updates, physical pointer/accessibility/mixed DPI and all real provider accounts remain distinct open work. The historical requirement matrix is not promoted to100% by these additions.


### r31 and r31a execution follow-up

Frozen r31 is SHA256 314647ba00b72f811513d6ff48c59d180fbf6613bbf96b34258df13d5f4bbf66,325 files. The connected PC completes GUI62 groups/79 PNGs and the CLI stages, with one Core failure out of1063 and0 skips. CI35688626212 at230b124ccaa0df5449cd1fed35764f0350410d41 also has one Core failure and skips later jobs. These failures are preserved separately: CI's early-parent test returns normally at226ms, whereas the PC times out after3 seconds without the expected normal-exit marker. Both produced parent/child startup and matching identity records.

The test-only r31a correction explicitly supplies standard output/error to the child by redirecting stdin, asserts the child's native PIPE type and successful write before startup acknowledgement, and retains the parent's process handle before observing its exit code. A GetProcessById object alone was not proof of a retained native handle. The early, detached and waiting cases keep their distinct outcomes and original3-second deadline/8-second return bound. This modifies only two fixture/test files; all323 other archive files match r31.

Frozen r31a SHA256 is bba1e570ce4abe617b04f03acc9a9d21a5f5f0bad2517d68ee5efba6c4223a26. Its QA head61596fe076d5514dba1ab0d8a965b1bd8c2b39cc completes both x64 and native ARM64 jobs in CI35689218538. The connected PC's fresh restore/Core/build stages exit0; final evidence hashes and detailed counts are independently checked before publication. Kimi Desktop and subsequent updater work are not included in this archive. r31's failed Core result is never relabelled as a pass; only unchanged product GUI/CLI evidence may be reused with the323-file comparison recorded.


Independent final r31a review binds all325 ZIP source hashes to QA Git blobs and the PC before/after records. CI x64 and native ARM64 each pass Core1063/1063 with0 skips and GUI62 groups/79 PNGs. Packaged PE architectures are0x8664 and0xaa64, and the native ARM64 job executes the checksum-verified archive built by the x64 packaging job. PC Core1063/1063 passes; the six PE cases take987ms detached,3002ms early,3006ms waiting,56ms echo,3048ms foreign launch and54ms flood. PC GUI/CLI evidence is explicitly reused from the unchanged323 inputs in r31. DeepSeek normal/Partial captures were inspected directly and API mode's no-additional-Web-request assertion passes. Physical hover/wheel remains INCONCLUSIVE; no real provider account is implied. Independent final report SHA256: 0d8177d9f80b0ce19f7b8ab379ea8cc45322dba0db7c21e68d9834d4e51c86b0.

During concurrent follow-up workers, one documentation edit was refused with EVIDENCE_INVALID: journal truncation or unfinished append. It did not apply the edit. After the other worker completed, the216-event journal and its head matched and the official summary read normally with INCONCLUSIVE. No journal/controller record was edited or repaired. Subsequent workers share an external serialization lock. The refusal and failed context-command invocation remain recorded; native checks do not override the formal harness result.
