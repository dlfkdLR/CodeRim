# Windows parity against the unchanged macOS implementation

The macOS working tree and running application on September 23 are the comparison reference. Later changes in that separate checkout are recorded below; this Windows task does not overwrite them. This task must not change macOS source, configuration, resources, installed application or user preferences. Widgets are excluded. A 389-file SHA-256 baseline protects the local Mac source/configuration/test/design files. This is an active comparison, not a declaration of 100% parity.

The reference includes local Mac changes beyond the published 2.1.8 source, including account-wide ChatGPT history. Copying only the public release is therefore insufficient. Windows changes start at `44947e9` and preserve the MSI/update implementation and existing user data. Independent source comparison used the actual local Mac files and the Windows 2.1.10 implementation, not only documentation.

## Difference register

| ID | Severity | Reference and Windows difference | Current state |
| --- | --- | --- | --- |
| WP-01 | Medium | SettingsWindowController: category title, unified toolbar, sidebar hide/show, content dimensions and frame restoration | Implemented; x64 reference checks pass in 35852865096; full visual/DPI review remains open |
| WP-02 | Medium | UsageSettingsOverview: unavailable local usage must have an empty state, not a numeric zero or fabricated Claude history | Implemented; x64 reference checks pass in 35852865096; full visual/DPI review remains open |
| WP-03 | Low | UsageSettingsOverview: breakdown icons, fixed 240-point breakdown, 42/20-point totals, Today cost and section spacing | Implemented; x64 reference checks pass in 35852865096; full visual/DPI review remains open |
| WP-04 | Medium | MenuPopoverView: detail navigation replaces provider/account header; Back restores the overview | Implemented; x64 reference checks pass in 35852865096; full visual/DPI review remains open |
| WP-05 | Medium | General/RefreshMode: Automatic is distinct from one-minute polling; eight modes; only Automatic uses file events | Implemented; eight choices and polling/file-event distinction pass installed/direct x64 and ARM64 in CI22 (36147470331) |
| WP-06 | High | AccountsPane.SignIn uses the current CLI login instead of the isolated temporary home used by both Mac account runtimes | Implemented isolated temporary home, preflight policy, verification, save without switching and cleanup; all ten isolated-login fixtures pass installed/direct x64 and ARM64 in CI22; real accounts remain unverified |
| WP-07 | Medium | Accounts view: Current indicator, disabled switching for current account, operation-wide busy state and activation refresh | Implemented; identity probes cancel and finish before account mutation; native verification pending |
| WP-08 | Medium | Mac Codex desktop quit/switch/relaunch integration versus Windows CLI-only switching | Open; do not silently terminate user sessions |
| WP-09 | Medium | Notch Task Activity: unknown tasks, state duration and per-chat tokens including subagents | Read-only remote catalogue, duration, optional background token totals and three native controls implemented; x64 toggles/computation/motion pass in 35852865096; parent/child groups, context parents, safe catalogue metadata and inline expansion pass installed/direct x64 and ARM64 in CI22 |
| WP-10 | Medium | Provider local statistics, Sources, database size/date range, pricing catalogue and Rebuild Statistics | Implemented strict bounded reparse, atomic replacement, async clear/rebuild and grouped statistics; installed/direct x64 and ARM64 receipts pass in CI22 |
| WP-11 | Medium | Providers rows: account/plan/limit state, contextual connection action and alert bell | Shared local identity/reading snapshot, stable rows, alert bell and connection actions pass x64 installed/direct native execution in 35856976739; remaining other-provider identity/routes are open; existing native rows pass both architectures in CI22 |
| WP-12 | Medium | Add Providers: two-column cards, summary search, empty search state, added count, Settings action and default Done | Implemented; x64 catalogue/search/keyboard checks passed in run 35848610879; full visual acceptance remains open |
| WP-13 | Medium | Claude connection lifecycle, detected identity and provider-specific analysis settings | Enable/detect/explicit link, unlink/disable, safe helper lifecycle and independent polling implemented; lifecycle/migration installed and direct fixtures pass x64/ARM64 in CI31; full visual detail and real-account checks remain open |
| WP-14 | Medium | Other provider details: limit progress/reset, identity/source, management link and per-provider alerts | Compact progress, future reset rows and synchronized alerts pass x64 installed execution in 35869577739; script/Copilot/GLM identity, source and fixed management links pass local regressions and installed/direct x64/ARM64 in CI22; remaining adapters/routes are open |
| WP-15 | Low | Notch fixed colors show hex values, missing System choice and contextual motion explanations | Named colors/System and live motion explanation implemented; also fixed ignored custom accent below 50% in Usage mode |
| WP-16 | Low | Information Build row, link-row treatment, Codenotch MIT link and independent-project notice | Implemented; layout, build identity and bundled notices pass installed/direct x64 and ARM64 in CI22 |
| WP-17 | Medium | General startup status must read actual OS registration on activation | Implemented OS registration/approval read, explicit repair, exact rollback and activation refresh; stale preference no longer re-registers on app launch; installed/direct x64 and ARM64 receipts pass in CI22 |
| WP-18 | Medium | Usage provider popup differs from current Mac popover/selection/search behavior | Searchable 272-point popover, 40-point rows, selected state and keyboard behavior implemented; installed/direct x64 and ARM64 picker fixtures pass in CI22; availability independent of notch membership and lifecycle fixtures pass installed/direct x64/ARM64 in CI31 |
| WP-19 | High | ChatGPT account totals, dated server history and separate device-local history are absent on Windows | Implemented dated account/local period views; x64 installed/direct native race, identity and calendar fixtures pass in 35854834026; injected clock/wake/identity races pass both architectures in CI22; physical OS signals and real account checks remain open |
| WP-20 | Medium | Full limits/analytics/projects/sessions/tooltip states and physical interaction parity | Active audit; no blanket pass from existing fixture coverage |
| WP-21 | Medium | AccountLimitsView: remaining metric/bars, quota card order, reset countdown, pace and freshness | Card layout, remaining progress, ordering, reported names, pace, clock and disclosure pass x64 installed checks in 35878689597; relative-time/font comparison remains pending; installed/direct account-limit fixtures pass both architectures in CI22 |
| WP-22 | Medium | CodexNotchProvider: Pro plan hides the five-hour quota when another quota exists | Implemented raw-plan filter for initial/live rings and popover, retaining the sole-quota fallback and all Usage quotas; installed/direct x64 and ARM64 receipts pass in CI22 |
| WP-23 | Medium | Reset credits belong to the separate Usage row, not the quota-only notch snapshot | Implemented dedicated numeric/Unlimited/Available/expiration row and notch filtering; installed/direct x64 and ARM64 receipts pass in CI22 |
| WP-24 | Medium | Companion display uses the Pro quota policy while persisted restart data must retain every quota | Implemented separate display quotas and optional owner-scoped restart cache with legacy fallback; local regressions and installed/direct x64/ARM64 restart/ownership checks pass in 36143000472 |
| WP-25 | Medium | Codex parser: remaining-only percentages, positive integer duration and duplicate-window handling | Implemented bounded percentage/duration/reset/deduplication rules; 21 targeted regressions pass; malformed/extreme-number and native integration boundaries remain explicit |
| WP-26 | Medium | Account freshness: Windows prematurely ages Claude at five minutes and Codex on reset expiry | Shared provider-specific freshness implemented; 21 regressions and installed/direct x64/ARM64 UI checks pass in 36143591151 |
| WP-27 | Medium | Local token tooltip: loading, unavailable, known zero, partial/stale, local-midnight scope and live independent updates | Implemented against the separately observed September 25 Mac source; local regressions and installed/direct x64/ARM64 state/refresh fixtures pass in CI26 |
| WP-28 | Medium | Settings window can start taller than its working area, clipping the frame | Reproduced in CI22 installed/direct desktop captures; size/position and display-change correction implemented; installed/direct x64/ARM64 frame cases pass in CI26; physical monitor transitions pending |
| WP-29 | Low | Popup uses a straight triangle instead of the Mac curved tail | Same two cubic curves and design-frame dimensions implemented; installed/direct x64/ARM64 all-edge/all-scale checks pass in CI26 |
| WP-30 | Medium | Provider tooltip title, account spacing, named groups, count-only rows and inline pace differ from Mac | Reference layout and reserved/deficit calculation implemented; Core35 and installed/direct x64/ARM64 fixtures pass in CI27; empty states/footer removal pass both architectures in CI28 |
| WP-31 | Medium | Cost subtotal/interval coverage, measured empty cost and cost-chart presentation differ from Mac | Model-wide coverage, empty/output-only cases and chart presentation implemented; Core40 and installed/direct x64/ARM64 fixtures pass in CI32; request-aware high-context pricing is tracked separately as WP-35; full presentation remains open |
| WP-32 | Medium | Analytics dates differ in locale joining words, spacing, year and clock format | Explicit Foundation patterns, overrides and fallbacks implemented; twelve fixed locales and five other date groups pass installed/direct x64/ARM64 in CI43 and CI45; every OS locale data set is not verified |
| WP-33 | Medium | Session Started and folder names are lost or change with selected range | Durable provider-isolated metadata and detail/list wiring implemented; ten native detail groups pass installed/direct x64/ARM64 in CI43 and CI45 |
| WP-34 | Medium | Analytics omits local Codex desktop task titles and generic-project name enrichment | Read-only in-memory labels implemented; five title groups and six local-data groups pass installed/direct x64/ARM64 in CI45 (36179678176); bounded fallback cases remain explicit |
| WP-35 | Medium | Proven high-context requests are excluded from cost estimates | Request-level proof, nullable storage and tiered estimator implemented; eleven focused cases included in Core52 (2,044 passes); thirteen cost groups pass installed/direct x64/ARM64 in CI46 (36181235816) |
| WP-36 | Medium | Claude agent transcript usage collapses into the parent session | Scoped child identities, copied-history migration and safe parent IDs implemented; four installed/direct native import/navigation groups pass x64/ARM64 in CI44 and CI45 |
| WP-37 | Medium | Projects/Sessions required a Show more action after 40 rows | Removed page state and quadratic metadata scans; seven installed/direct list groups pass x64/ARM64 in CI47 (36183247683); arbitrary-size performance remains unmeasured |

## First change batch

Windows-only files: `DashboardWindow.Shell.cs`, `DashboardWindow.cs`, `UsagePane.Overview.cs`, `UsagePane.cs`, `UsagePane.Timeline.cs`, `AppSettingsStore.cs`, `App.xaml.cs`, `Ui.cs`, and native smoke assertions. The custom caption retains WPF WindowChrome system drag/resize behavior and explicit close/minimize/maximize commands; screen/DPI, maximized geometry and keyboard/high-contrast behavior require native checks. Frame restoration is separate from account/configuration files and checks whether its caption remains on an actual monitor.

Legacy 60-second settings retain Automatic behavior. Explicit one-minute selection writes the same polling interval with file events disabled. Old 30-second/5-minute modes migrate to timed polling; unknown intervals continue to use the established default. Manual mode stops polling and filesystem watching.

Native regression assertions exercise sidebar hiding/restoration, category titles, all eight refresh choices, Today cost, provider dimensions, detail/Back hierarchy and empty Claude history. Build success alone does not close the visual requirements. Existing x64/ARM64 2.1.10 results are baseline evidence, not evidence for this change.

## Validation record

- Initial build found one analyzer error (instance method that must be static); corrected without suppressing the analyzer.
- Latest full Windows solution build (including isolated sign-in and its native assertions): zero warnings and errors. Native execution remains pending.
- Core baseline on this Mac: 1,626 passed, 31 Windows-only skips, no failures. The added native environment-isolation test is not counted as locally verified.
- Independent review found an account-probe/switch race, late managed-policy checking and silent temporary-credential cleanup failures. All three received source fixes and require native verification.
- Add Account fixture covers Codex and Claude success, cancellation, failed CLI, ignored isolation and wrong account. It never signs into a real account.
- Development harness `run-139171372d9448b2b9b2d4842ca00abf`: baseline native build/test adapter unavailable. Normal .NET/Windows CI results are recorded separately; no formal ACCEPT is claimed.
- Mac source preservation check and final independent review are required before application to the user's checkout.

No total match percentage is assigned while the inventory and open functional differences remain unresolved.

## Second change batch and native findings

- Provider catalogue now follows the 600 × 520 Mac content frame, two card columns, stable initial ordering, description search, empty state, added count and Add → Settings action. The Usage popover follows the 272-point width, 40-point rows and explicit keyboard selection/cancellation.
- Independent review found missing light-mode logo foreground, focus/highlight disagreement, Clear search key interception and stale reduced-motion help. These received source fixes and new native assertions; successful native execution is still required.
- Fixed a functional ring bug: the chosen accent was ignored in Usage colors mode. The accent now applies below 50%; yellow/orange thresholds remain unchanged. System and named colors retain existing saved hex colors.
- Native x64 run `35846973991` at `d048892`: core tests and builds passed, MSI install/upgrade progressed to installed-app execution; the UI run failed at the new Today-cost assertion. Isolated sign-in's ten synthetic cases passed. ARM64 was skipped because x64 failed. This run is FAIL, not parity evidence for the later picker changes.
- The new Usage overview test explicitly enters Token Usage and captures its screen/state before checking the cost. The cause and fix must be confirmed by the next native run.
- Latest local core run: 1,627 passed, 33 native-Windows-only skips, no failures. Full Windows cross-build: zero warnings/errors.

## Third change batch: task activity and evidence corrections

- Native x64 run `35848610879` passed the provider catalogue and searchable Usage picker assertions, plus ten isolated sign-in fixtures. It failed at the same Today-cost assertion, so ARM64 did not run. The captured state proves cost estimates were enabled and the overview was visible.
- Root cause of that assertion: the synthetic GPT-5.6 fixture omitted cache-write usage, which the pricing policy correctly treats as unknown. The fixture now explicitly supplies zero cache writes. A separate native assertion verifies missing cache-write data still produces no invented cost. Production pricing behavior is unchanged.
- Task Activity now has the Mac's three options. Unknown remote tasks come from a read-only, schema-checked local catalogue; their timestamps are never presented as task duration or token measurements. Busy/waiting duration matches the reference's minute/hour/day formatting.
- Local per-chat totals include child usage once, leave unmeasured/remote tasks blank and reject invalid or overflowing totals. An independent review caught synchronous UI-thread aggregation and mismatched filename/metadata identity. Aggregation now uses cancellable background snapshots; disabled options cancel in-progress work and release caches. Activity identity now follows the importer's first complete metadata record and normalized filename fallback.
- Regression coverage connects real JSONL import to activity identity for renamed files, conflicting filename IDs, uppercase UUIDs and no-UUID fallback. Native checks exercise the actual toggles, dispatcher availability while history is paused, stale result rejection, disabling an in-progress calculation and optional popup metrics. Parent/child visual grouping is still open.
- Local full Windows solution build: zero warnings/errors. Core tests: **1,644 passed, 33 Windows-only skipped, zero failures**. New native scenarios remain pending until the next Windows run.
- The capture helper now bounds its VisualBrush to the actual viewport. The old automatic brush bounds compressed popovers containing offscreen rows, so those earlier popup images are insufficient visual evidence even though interaction assertions passed.

## Fourth change batch and third native run

- Native x64 run `35850710641` at `fbc695e` passed the new Mac-reference shell/refresh/Usage assertions, including priced and unpriceable Today data, provider dimensions, overview/detail/Back and empty Claude history. Task Activity toggles, background computation, stale result rejection, cancellation on disable and optional metrics also passed. Their JSON and captures are retained with the run.
- The full run then failed in the older project navigation test because it still searched for the former `‹ Back` text. The existing Back button is now an icon with the stable `usage.navigation.back` identifier. Both obsolete selectors were updated; the search/period restoration assertions remain. ARM64 was skipped. This run is still FAIL overall.
- Actual popup captures exposed centered task rows. The task button now stretches its two-line content, with title/state above detail/metrics. Added the Mac's three-quarter working ring with a 1.4-second rotation, static half waiting ring and full idle ring. Unknown tasks have no ring. Hidden/unloaded/reduced-motion states release the render subscription; native motion checks cover these transitions.
- Information now includes Build, four project links, the Codenotch MIT notice and independent-project copy. The Windows package includes unmodified repository LICENSE/NOTICE as text files. Data-scope/privacy copy remains truthful to the still-unimplemented account-total integration rather than claiming a feature prematurely.
- General now reads the actual current-user Run entry on activation. Known Windows disabled approval is respected and an unknown approval encoding stays explicit; only Windows Settings can change that approval. An explicit user toggle can repair a deleted Run registration even if the old preference says enabled. The app's previous automatic re-registration on launch was removed, preserving external user changes.
- Settings persistence rollback now restores the previous Run value and registry kind, not a guessed value from stale JSON. Native fixtures restore Run, approval, preferences and temporary paths independently even after a check/cleanup failure.
- Microsoft documents the [Startup Apps settings URI](https://learn.microsoft.com/en-us/windows/apps/develop/launch/launch-settings) and [Run startup locations](https://support.microsoft.com/en-US/Windows/Experience/Startup-Boot/configure-startup-applications-in-windows). StartupApproved decoding is a read-only compatibility path, not a claimed public Windows API; unknown layouts are never assumed enabled.
- Mac preservation recheck: **389 files unchanged**. This batch is Windows-only and does not replace or relaunch the user's Mac app.

## Fifth change batch: account history and native results

- `35852865096` at `16fbaa6`: x64 core/build/package, installed MSI lifecycle/rollback and direct native UI/motion checks passed. Actual captures confirm the information link rows and corrected task alignment. This is synthetic Windows execution, not validation on the user's connected PC.
- ARM64 core/build progressed to installed-app UI and failed at startup reactivation (`Activation failed to read external registration, or wrote preferences while refreshing`). That full run is FAIL. The fixture now requires actual native window activation and records UI, preference, registration and approval state before its assertion. The cause is not yet established; production approval decoding has not been broadened without evidence.
- Account history now follows the current Mac's enabled default, fixed read-only profile endpoint, 20-second request timeout, redirect/cookie rejection, 1 MiB response bound and memory-only snapshots. The Windows credential loader checks bounded, non-reparse paths and the opened file's owner/access ACL without modifying CLI credentials. Synthetic and injected provider fixtures remain offline unless an explicit profile fetch fixture is supplied.
- Local Today remains device-scoped. Account History displays dated weekly/monthly/lifetime totals without adding local events again. Account and local period details have separate totals, with a clearly local input/cached/output breakdown. Unavailable account data is a dash, not fabricated zero.
- Identity checks use workspace plus access-token subject. Refresh, identity polling, disable, account operations and calendar changes invalidate incompatible state. Successful and failed requests both check the current context before accepting or retaining a snapshot. Account identity invalidation bypasses keyboard-focus refresh deferral and clears old numeric animation values.
- Core profile tests rejected a missing time-zone marker that the first decoder accepted; explicit ISO timestamp formats fixed it. Current full Core result: **1,670 passed, 33 native-Windows-only skips, zero failures**. Cross-target Windows solution build succeeds without warnings/errors. Native profile fixtures cover explicit enable, separate totals/details, same-workspace account switches, delayed success/failure, focus privacy, credential loss, week/day changes, busy account operations and disabling in flight; execution is pending.
- Mac source and installed app remain the fixed reference. No 100% match or public release is claimed for this branch.

## Sixth change batch: provider rows and ownership

- `35854834026` at `ae7a0e7`: x64 core/build/package, installed MSI lifecycle/rollback and direct UI/motion passed. Account History fixtures passed in both installed and direct execution; actual overview/detail images were inspected. These are injected account responses, not live account validation.
- ARM64 failed because the settings window did not regain native activation after the owned fixture became active. The captured registry status was Enabled while the unactivated UI remained Disabled. This identifies the failed transition, not a production approval-decoding bug. The revised fixture hides its owned window before returning to its owner and requires both native IsActive and a new Activated event. It always saves transition diagnostics, including on failure; no event is synthesized. Native rerun is pending.
- Provider rows now show the current account, plan and relevant limit with a persistent alert bell, compact Details/Set Up action and remove control. Muting persists in place; global alert disable is respected; removing a provider stops monitoring without deleting saved state. Other-provider identity metadata and provider-specific sign-in routes remain open.
- Independent review found a privacy/correctness race: a new CLI email could appear beside a previous account's cached quota. A shared AccountDisplay snapshot checks the exact local identity against the captured reading scope; provider rows/details, Usage limits, notch cards/account menu/rings and picker availability use guarded readings. Each label-plus-quota render consumes the same snapshot. External same-workspace switch and sign-out fixtures are added, but have not executed on Windows yet.
- Codex/Claude rings and threshold alerts now share the Mac rule of choosing the highest used percentage. Previously a low first/session limit hid a high weekly limit from 80/100-percent alerts. Other providers retain their specified window order. Core regressions verify both rules and no-percentage fallback.
- The actual x64 capture showed a clipped Usage refresh glyph. Its 28-point button now reserves enough content space and fits the entire stroke; native bounds assertions accompany the correction.
- Local solution build: zero warnings/errors. Core tests: **1,673 passed, 33 Windows-only skipped, zero failures**. This batch's Windows execution is pending; existing run results do not verify uncommitted changes.

## Seventh change batch: local data and calendar invalidation

- `35856976739` at `085fbe0`: x64 installed/direct provider rows, account reading ownership, complete refresh glyph and full UI/motion checks passed, as did build/package/MSI lifecycle. The actual provider row and Usage captures were inspected. ARM64 still failed at startup activation: both Window.Activate returns were false, dashboard had no Activated event and actual registration was Enabled. No production startup bug is inferred from that failed transition.
- The startup fixture now tries documented [SetActiveWindow](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-setactivewindow) for its own UI-thread windows when foreground activation is denied. It still requires a new native Activated event and IsActive on both windows, and exercises the production handler. Native thread activation and actual foreground ownership are recorded separately. A result with foregroundConfirmed=false cannot establish foreground/input E2E parity. This change is awaiting native execution.
- Profile History now handles system clock changes and resume on the UI dispatcher, clears the cached time zone and rejects old in-flight totals even after a backward same-day clock change. Disabled/disposed stores do not refetch; static system-event subscriptions are removed at disposal. The native fixture uses background callbacks, not a real OS clock change or physical sleep/wake. The integration follows Microsoft's [TimeChanged](https://learn.microsoft.com/en-us/dotnet/api/microsoft.win32.systemevents.timechanged) and [time-zone cache](https://learn.microsoft.com/en-us/dotnet/api/system.timezoneinfo.clearcacheddata) documentation.
- Provider settings now have Local Data, Sources and Manage Data sections with discovered file count, shared database/WAL size, provider record dates, account-limit status, pricing catalogue version and Open Data Folder. Clear and Rebuild run database work off the UI thread and share the local import lock. Repeated identical operations join the same task; conflicting operations are rejected.
- Rebuild scans fresh, bounded source data before atomically replacing derived rows and session/attachment metadata. It preserves other providers, deletion cutoffs and explicit exclusions. Normal import retains its existing tolerant policy. Rebuild rejects inaccessible/reparse roots, malformed or incomplete accounting records, unfinished/resource-limited scans, an empty individual source and a wholly unusable source inventory, retaining the prior statistics. Original session files are never edited. Missing subsets of old source files are not inventoried, and an already-running SQLite write is not guaranteed to stop exactly at the scanner timeout.
- Independent review caught the normal importer's silent malformed-line skips in the initial rebuild implementation. Strict parsing now checks every nonblank line; per-file completeness also prevents one valid file from hiding another empty file. Normal non-accounting messages and blank lines remain accepted. Regression tests cover corrupted Codex/Claude lines, valid/empty mixed sources, rollback after partial insertion, other-provider isolation, corrected counters, statistics persistence and clear exclusions.
- A new positive Codex test initially omitted last_token_usage, so the existing normalizer correctly marked its first cumulative-only reading partial. The fixture now supplies both counts; production normalization was not loosened. Final local results and new installed/native fixtures are recorded after execution.
- Full Core12 run had one timeout in the existing Kimi optional-transport test (1,687 pass, 33 skip, one fail). The unchanged Kimi test class passed all 24 tests when rerun separately. That first failure is retained as timing-test instability evidence; the final full-suite rerun is recorded separately.
- Independent review also caught the strict rebuild scanner being retained for normal imports. Successful rebuild now creates a fresh normal-policy scanner, preserving tolerant incremental reads while discarding the obsolete cache.

- Core13 failed a different existing wall-clock test: Alibaba isolated-child output overflow reached its two-second deadline under the parallel suite (1,687 passed, 33 skipped, one failed). Its unchanged class passed all 24 tests separately. The process tests now join the existing nonparallel native-process collection, and Kimi real-deadline tests use a nonparallel transport collection. Product deadlines and exact exception/assertion requirements are unchanged. Core14 with that scheduling isolation passed **1,688 tests, 33 Windows-only skips, zero failures**; build55 had zero warnings/errors. Prior failed runs remain recorded.
- Independent source review closed both rebuild findings after checking strict corruption detection and restoration of the normal scanner policy. Native execution for this batch is still pending.

## Eighth change batch: reference defaults and grouped task activity

- Native run `35860398971` at `f83ae13` passed x64 Core/build/package/MSI lifecycle and installed/direct full UI execution. Installed JSON records confirm strict Local Data rebuild and preservation, clock/resume invalidation, and actual foreground startup activation. ARM64 also passed Core, native MSI lifecycle and direct full UI/CLI execution; activation and new Local Data/clock fixture artifacts are being inspected separately. The Local Data image only brought the heading into the bottom of the viewport; its next capture now scrolls the section to the top. The initial image is not evidence of a fully visible Local Data section.
- New/missing Windows preferences now use the Mac defaults for cost estimates, completion sounds and reset-date formatting. Explicit saved false/countdown preferences remain unchanged. Reset picker order/copy match Reset date and Time remaining. Claude no longer exposes an unsupported cost estimate toggle; provider analytics notes and dependent enablement follow the reference. Native fixtures test real persistence and toggles, but have not yet run for this batch. Native system sound assets still differ between platforms.
- Task cards group children under their main chat, including a display-only parent when that chat is no longer active. Parent context does not enter completion detection or running-task counts. A bounded, schema-checked, parameterized, read-only lookup of the local Codex state database supplies titles and main-parent relationships; cycles cannot hide or recurse through tasks. Remote/local identities stay separate. Desktop display-title overrides, saved project names and remote sub-agent metadata are still open.
- Parent totals include local child usage once. Child rows have indented titles and parent-aware accessibility labels; unknown/unmeasured values stay blank. The card expands/collapses in place, and expansion survives clock/store refresh. Focused task/disclosure controls refresh using stable automation identities while preserving focus and scroll; native pointer/keyboard behavior for this batch remains to be executed.
- Independent review caught stale focused popup content and a native fixture's lower/uppercase usage-identity mismatch. Both received source fixes, and the fixture now exercises live task changes in the production popup while its disclosure retains keyboard focus. Initial Core group tests also reproduced an uncaught invalid-schema exception; the optional catalogue now safely retains live activity without metadata. The targeted rerun passed nine tests.
- Build65: zero warnings/errors. Core16: **1,698 passed, 33 Windows-only skipped, zero failures**. An added uppercase-parent regression preserves the importer’s raw usage identity while using normalized UUIDs for display grouping. Earlier build56 analyzer errors and build61 fixture assembly-visibility errors were corrected without relaxing analyzers or exposing a private hash API. Source hash verification again confirms **389 Mac files unchanged**. No latest native PASS, release or total parity percentage is claimed until the matching commit executes.

- The focused-popup and fixture-identity findings are independently reviewed as source-resolved. This is not a native acceptance of the newest batch.

- Inspected ARM64 installed/direct artifacts from 35860398971 confirm native Arm64 OS/process, completed Local Data and 19-call profile fixtures, and four production Activated handler transitions. All four foregroundConfirmed values are false on that hosted runner, so this verifies thread-window activation, not foreground/input E2E. x64 recorded foregroundConfirmed=true. These are GitHub runners, not the user’s connected PC.

## Ninth change batch: Codex detail structure and current-login plans

- Codex details now retain the reference Account, Limits, Usage Analytics, Local Data, Sources and Manage Data sections, without duplicate quota, connection and order blocks. Its account note leads to the existing Usage account menu. Windows executable selection remains available under Diagnostics. macOS source remains untouched.
- Account rows and popovers use the same current-login identity for label and plan; a changed identity still hides the previous account's readings. Codex Pro 5x/20x display names follow the Mac mapping. Synthetic native assertions require the new plan to appear in both provider row and popover, reject old quota, cover Pro 5x and clear data on sign-out. These assertions await native execution.
- Independent source review found no new blocker in the detail/plan delta; real account retrieval, missing-plan row transitions, file-picker interaction and complete visual equality are not implied.
- Native CI8 (`35862566151`, `e810464`) stopped at Core: **1,730 passed, one failed**. The failure occurred before product metadata lookup: the test's setup SQLite writer held a Windows handle while `File.ReadAllBytes` attempted the initial snapshot. The fixture now closes its writer before both byte-comparison snapshots; read-only product code and byte-equality assertions remain intact. Build68 passed with zero warnings/errors; all ten ActivityGroups tests passed locally after the fixture correction. Native UI/MSI checks were skipped in CI8 and are not counted as passed.

## Tenth change batch: task title and project metadata

- Local task names now follow explicit saved name, local desktop display title, stored title, then agent nickname. The optional project table supplies saved project names. Generated Documents/Codex/date/task workspaces hide their generated folder name; real project subdirectories and short ordinary project names remain visible. Remote catalogue rows use the same projectless rule while keeping unknown activity and excluding remote child records, as the reference does.
- Desktop title lookup is restricted to the local host: another host's copied UUID cannot rename a local task. Both optional metadata sources remain bounded, schema-checked and read-only; missing, invalid or unsupported metadata retains the existing task and live state. Cancellation is propagated. No credentials or user settings are written.
- Regression fixtures verify title precedence, archived parent title resolution, source nickname, saved project precedence, local/remote collisions, projectless path boundaries, malicious view rejection and byte-for-byte DB preservation. Build69 first failed the CancellationToken-last analyzer; the corrected overload preserves existing call sites. Build71 passed with zero warnings/errors. Core17 passed **1,707 tests, 33 Windows-only skips, zero failures**. Independent source review found no new blocker; this metadata batch has not yet executed on Windows.
- Source preservation check9 again found **389 of 389 Mac reference files unchanged**. This branch remains unreleased, and no total equality percentage is claimed.

- CI9 (`35863792942`, `95f25b6`) passed Core/build/package but failed installed native UI at the new positive popover-plan assertion; subsequent direct UI and ARM64 stages did not run. The popover was not mounted in a Window, so the ScrollViewer template's content was absent from visual-tree traversal. The fixture now mounts/shows it, waits for layout, then requires Pro 20x and absence of the previous account's plan/quota, and captures that actual window. Earlier negative checks on an unattached popover are insufficient privacy evidence. Provider row assertions did run before this failure. Product account ownership policy was not relaxed. Build72 passed with zero warnings/errors; corrected native execution is pending.


## Eleventh change batch: provider usage and alert controls

- Generic provider details now render grouped compact four-point usage bars, the reference percentage summaries, nonnumeric balances and only future reset dates. The nearest future reset is shown below the section. Redundant per-detail notch-order controls were removed; provider list reorder/removal remains available. Connected headers now have the same mute bell and a synchronized Alerts toggle; disabling global alerts disables both without replacing unsaved credential controls.
- Independent review found four issues in the initial delta: lost fractional/over-limit summaries, stale alert UI after a failed save, fixed brushes under high contrast, and lost tooltip count-format preferences. All received source fixes. Percentages such as 0.3%, 99.7% and 150% keep their real values; only bar/ring geometry clamps to its range. Ring Remaining mode retains fractional remainders. Tooltip count-only values respect Compact/Detailed preferences while settings summaries keep the reference compact convention. Existing bars update through dynamic theme resources. Native alert save-failure injection and actual OS high-contrast switching remain unverified.
- The unchanged Mac generic reset row currently repeats its `Resets` prefix. Windows retains one prefix; this explicit grammar difference is not called literal text equality. The Mac source was not edited.
- New native assertions cover actual progress indicator width, exact/unknown/balance summaries, future reset filtering, header/toggle persistence, global disable, credential draft retention and clearing stale account readings. Ring property/accessibility assertions do not independently prove rendered pixels. Existing connector fixtures retain connector-to-render checks by asserting the actual bar value instead of obsolete percentage text that conflicts with vendor-provided display values.
- Build77 passed with zero warnings/errors. Core19 passed **1,730 tests, 33 Windows-only skips, zero failures**. Independent source review closed its findings; latest Windows execution is pending. Mac preservation check10 found **389 of 389 files unchanged**.
- CI10 (`35865118312`, `9700f9c`) passed Core/build/package and the corrected mounted account-ownership check. Its actual popup capture shows the new account's Pro 20x and no old quota. The overall run failed at the reset picker labels assertion, before provider-preferences completion and later task-group checks; ARM64 did not run. The assertion had inspected a detached DataTemplate. It now opens the actual ComboBox and checks both rendered item containers, captures the popup, then tests persisted selection. Expected labels and persistence requirements are unchanged. A new native run is required to confirm the correction.


## Twelfth change batch: reversible Claude helper integration

- The Windows helper installer now keeps a bounded, private recovery record beside the selected Claude settings. Disconnect restores the first-captured status line only while the current command is one installed by this integration. It removes only CodeRim SessionStart handlers, preserves other hooks/settings and later user-edited status lines, and supports moving/reinstalling the package. Old installations without recovery state remove identifiable CodeRim commands without guessing an original from historical backups.
- Recovery is journaled before settings replacement and tracks the old/new command during a package move, so interruption does not strand the original configuration. Cooperating installers use a per-configuration file lock. Expected-content checks reject an intervening settings write and retain recovery information for retry. These checks are not a universal OS-level compare-and-swap guarantee against noncooperating writers in the final check/replace interval. Reparse ancestors and unreadable/unsupported recovery records fail closed. Generated settings and recovery JSON must fit the same byte limit used by the reader.
- `CodeRimCLI.exe claude-disconnect` exposes helper removal/restoration; it does not yet implement the full Mac Enable/Add Account/Disconnect UI lifecycle. Existing credentials and local statistics are retained. Windows README identifies this as development source, not a published release.
- Independent review identified JSON null settings escaping the handled error path. Settings roots now require a JSON object across approval detection, install and uninstall. Null/array/boolean fixtures fail with InvalidDataException without modifying settings. Build81 passed with zero warnings/errors; the final helper fixture class passed **18/18**. The prior full Core20 run passed **1,745 tests, 33 Windows-only skips, zero failures** before those last three invalid-root cases. Native helper/CLI restoration checks have been added but have not run on Windows yet.
- CI11 (`35867587644`, `1417f7e`) passed Core/build/package but failed installed UI earlier in the Antigravity regression. The saved connector JSON records `32.99999999999999` for 33%, while the changed bar assertion used exact equality with 33. All three equivalent connector-to-bar assertions now use the same 0.001 tolerance as the existing connector assertion. Product calculation/rendering is unchanged. The reset picker, provider-detail and task-group scenarios did not execute in this failed run; ARM64 was skipped.
- Mac preservation check11 again found **389 of 389 reference files unchanged**. Full parity remains open and this branch remains unreleased.


## Thirteenth change batch: elapsed status, Claude plan labels and task notifications

- Stale provider rows/details now show the reference relative Last read wording. Shared elapsed formatting handles future timestamps, the 45-second threshold, minute rounding and hour/day boundaries; active task durations retain their enabled/state guards. Claude Max 5x/20x labels use only recognized profile tiers whose email and organization match the same local login snapshot. Saved credential identity remains unchanged. CLI auth-status tier precedence is still open; a local profile match is not independent server-account verification.
- CI12 (`35869577739`, `91e279b`) passed Core/build/package and x64 installed helper/CLI restoration, provider preference/reset-menu and provider detail regressions. Actual JSON and captures confirm the rendered reset menu, progress widths/percentage summaries, alert persistence and draft preservation. It then failed the focused expanded task live-update assertion; later direct UI and ARM64 checks were skipped. This run remains FAIL overall.
- The failing fixture called UpdateSessionActivity directly, bypassing the Changed notification that the production ReadActivityAsync caller already supplied. This is not evidence that normal production polling failed. Notification is now issued at the mutation boundary, with the duplicate production notification removed. Native regression requires one notification for a changed snapshot, none for the same snapshot, updated visible task text and preserved disclosure focus. It retains the original timeout/assertions and records diagnostic booleans even on failure. New Windows execution is pending.
- Build85 caught a new fixture local-variable shadow error; the corrected build86 passed with zero warnings/errors. Full Core22 passed **1,768 tests, 33 Windows-only skips, zero failures**. This includes 13 elapsed and seven account-plan cases. New mounted native fixtures also require stale row/detail wording and Claude Max 5x/20x popover labels without changing raw login identity. Cross-target compilation is not proof of their native execution.
- Independent source review found no blocker in the elapsed/plan delta. Mac preservation check12 confirms **389 of 389 reference files unchanged**. The branch remains unreleased with open lifecycle and visual comparison work; no 100% claim is made.


## Fourteenth change batch: provider connection layout

- Provider headers now share the existing Usage pane's 28-point, borderless vector refresh button, keeping its accessible name and async duplicate-click guard. Removed the repeated Refresh/Setup guide toolbar beneath usage. The original guide destination remains available as Connection instructions in the connection card.
- Nonlocal provider credentials, source/region pickers and browser import controls now sit inside a Connection settings card. Existing control objects and handlers are moved once while constructing the page; polling still updates reading labels/bars in place, without rebuilding drafts. Connection actions keep their content width. This is layout work; generic account/source metadata and provider-specific routing remain open.
- Independent source review found no new blocker and traced source selection, secret saving, imports, draft preservation and the remaining Claude setup instructions. Builds88/89 passed with zero warnings/errors. The native detail fixture now checks both-axis refresh glyph bounds, one refresh toolbar, mounted credential controls, guide destination, preserved drafts and a separate connection-card capture. Native execution for this batch is pending; CI13 at 37e5253 covers the preceding batch only.


## Fifteenth change batch: task UUID lookup

- Real SQLite fixtures reproduced two missing-name failures when activity/parent UUIDs and stored UUIDs used opposite upper/lower spellings. State and local desktop title lookups now include the original, lowercase and uppercase spellings as bound equality parameters, retaining index-eligible lookups without applying a function/collation to every stored row. Conflicting state rows for one normalized UUID fall back to the original activity instead of selecting an arbitrary parent/title. Arbitrary mixed-case database spellings beyond those three forms are not a newly claimed compatibility guarantee.
- The failed before run is retained (**two failures**). Build92 passed with zero warnings/errors; the complete ActivityGroups class passed21 tests, and build93 plus the final class passed **22/22**, including conflicting UUID spellings. Fixtures verify raw live/usage identities, state and timestamps are unchanged and both SQLite files remain byte-identical. Independent review checks query bounds and read-only/local-host restrictions; native execution of this new delta is pending.
- CI13 (`35871830014`, `37e5253`) passed x64 Core/build/package and installed native elapsed/Claude Max labels, grouped task refresh/focus, profile History and Local Data checks. Actual task diagnostics record one notification, an open popup, updated text and focus on Show less. It subsequently failed the older popup reset-credit text assertion; direct full UI and ARM64 were skipped. The entire run remains FAIL. The grouped connection-card change at c1fe5d7 was not in CI13.


## Sixteenth change batch: reset-credit surface parity

- CI13's old assertion expected `2 resets` in the notch; the generic reference count formatter produced `2 left`. Source comparison exposed a larger contract mismatch: the frozen Mac NotchLimitAdapters maps only quota windows, while AnalyticsViews and MenuPopoverView render reset credits separately. Windows now follows that split. It no longer puts reset credits in the notch ring/popover data, and Usage shows one read-only Reset credits card with a full formatted number, Unlimited or Available plus the reported local expiration date. Generic provider count formatting is unchanged.
- Credit filtering happens before notch freshness evaluation, so a credit's expiration does not mark otherwise fresh quota windows stale. Existing disabled-account and reset-credit display preferences still gate the Usage row. No reset/credit-consuming action was added.
- The old popup-only text assertion is replaced by quota-only assertions across all edge/scale combinations and a mounted Usage regression for zero, two, two thousand, unlimited, available and expired values under both number styles. It checks one visible credit row, exact count text, read-only controls, local expiration month/day, no leaked credit/stale text in the notch, hiding preferences and independent fixture cleanup. Latest native execution remains pending.
- Build94 found two analyzer violations in the new UI/fixture; these were corrected without relaxing rules. Builds95/96 passed with zero warnings/errors. Core23 passed **1,771 tests, 33 Windows-only skips, zero failures**. Source review found no blocker in the credit surface split; full Limits layout and Pro-specific notch filtering remain explicit open work. Mac preservation check13 again found **389 of 389 reference files unchanged**.

- A tabular-numeral request was added to the credit count to follow the Mac row. Build97 caught the missing Typography namespace; the qualified call was corrected and build98 passed with zero warnings/errors. This requests the OpenType feature, not proof of every installed font’s glyph metrics.


## Seventeenth change batch: Pro quota visibility

- The notch now applies the frozen Mac rule to the raw plan from the same local login snapshot used for ownership: only Pro removes 270–330-minute windows, and only when another reported quota remains. Pro 5x (`prolite`), unknown plans and the only-quota fallback retain the reported data. The human-readable Pro 20x label and a cached reading's plan are not used as authorization or plan evidence. Both initial and live rings and the popover apply the policy before freshness evaluation. Usage's unfiltered account quota data is retained.
- New Core cases cover duration boundaries, primary headline selection, case/space plan normalization, unknown and display-only plan labels, zero-use single quotas and empty data. Build99 passed with zero warnings/errors; Core24 passed **1,781 tests, 33 Windows-only skips, zero failures**. Native assertions extend the real isolated login fixture to inspect initial/live rings and mounted Pro/Pro 5x popovers, a hidden expired five-hour reset, unchanged Usage data and the only-quota fallback. Their Windows execution is pending.
- CI14 (`35875278939`, `5073011`) runs the preceding grouped connection, UUID and reset-credit batches. It does not contain this new Pro delta. The Mac companion publisher also filters Pro windows; Windows' companion file currently doubles as the restart cache, so changing that persisted raw reading would lose Usage data. Its separate presentation mapping remains an explicit open item, not silently counted as parity.


## Native CI14 follow-up

- CI14 (`35875278939`, `5073011`) passed Core/build/package and installed provider preferences, then failed the new provider refresh geometry assertion. It did not reach the latest reset-credit regression or ARM64. The error and MSI failure log are retained; no actual icon coordinates were recorded by that old assertion, so the exact failed predicate is not established.
- Independent review of WPF Shape's Uniform layout confirms that declared 16-by-16 dimensions do not require both rendered axes to equal 16. The fixture now separately checks declared dimensions, positive bounded rendered dimensions and the actual rendered endpoints within the 28-point button. It records the geometry and a capture after layout, before asserting. Product refresh geometry is unchanged. Native re-execution must determine whether any real clipping remains.


## Eighteenth change batch: account-limit cards and live status

- Codex/Claude Usage limits now have compact rounded cards: account limit name and period, a remaining-percentage metric/bar, reset countdown with an absolute-time tooltip, pace estimates and a separate credit row. Primary Codex weekly quota is listed first; additional quotas obey the existing preference, including mixed-case Codex bucket names. The source's Pro filter remains specific to the notch, so Usage still shows both reported windows. Generic provider details retain their existing behavior.
- Optional bounded `limitName`/`modelName` metadata now survives parsing and the existing snapshot/cache round trip; older snapshots without it retain a friendly fallback. This additive presentation field does not replace window IDs, counts, usage percentages, credentials or account ownership. Broader Codex parsing differences discovered during this comparison remain WP-25.
- About these limits is a read-only disclosure. Empty, loading and unavailable states are explicit. A small loading indicator follows the motion policy and stops when unloaded/hidden. A 30-second UI clock updates reset, freshness and pace in place; stale/unsupported data does not show a pace estimate. The clock does not make network requests. The initial read-on-open lifecycle remains part of the provider integration audit.
- Independent review found a same-owner poll bypassing the keyboard-focus guard and a generic Limits page unnecessarily rebuilding on clock ticks. Those paths now use ordinary deferred refresh for the same owner and exclude generic pages. The owner key comes from the same login snapshot as the visible reading; account changes/sign-out immediately clear old private content even while the disclosure is focused. Native fixtures cover both cases rather than checking only a time-only callback.
- An extreme `9999-12-31`/+14:00 pace case reproduced an actual `ArgumentOutOfRangeException`. Adding in UTC fixes the local-clock overflow without changing the projected instant. The failing regression log is retained. Build102 caught an incorrect automation-peer call; it was corrected through IToggleProvider. Latest full build107 passed with zero warnings/errors. Core25 passed **1,822 tests, 33 Windows-only skips, zero failures**, including 41 new presentation cases. Later WPF fixture cleanup changes received build107; they do not alter Core calculations.
- Native fixtures cover parsed metadata, rendered remaining fill, weekly order, disclosure interaction and focus, owner invalidation, virtual clock aging, minimum-width light/dark/contrast cards, additional-window preference, empty/loading/unavailable states and indicator cleanup. They restore prior mode and disclosure state. Latest Windows execution is pending; CI15 (`35877668146`, `ba30a32`) covers the preceding Pro/refresh-geometry batches only. No assertion here is claimed as physical keyboard or all-DPI verification.
- Mac preservation check14 again confirmed **389 of 389 reference files unchanged**. Actual cross-platform glyph metrics, SwiftUI relative-run-out wording, the complete provider lifecycle and release publication remain open; this branch is not a 100% parity release.


## Nineteenth change batch: Codex response parsing

- Codex parsing now accepts remaining-only percentages, prefers an explicit used percentage, clamps Codex percentages, and requires positive integral durations. Duplicate duration/reset windows collapse within each bucket, while independent buckets and reset times remain distinct. Numeric reset timestamps use seconds (including epoch zero and long-range dates), avoiding the generic milliseconds heuristic. Credit counts retain exact integer tokens through Int64, clamp negative counts to zero, and respect first-present-field precedence. Other provider parsers are unchanged.
- Targeted fixtures reproduced **19 failures out of 21** before the change and pass **21/21** after it. Build110 passed with zero warnings/errors; Core26 passed **1,843 tests, 33 Windows-only skips, zero failures**. Independent source review found no blocker for the bounded normal-response path. This does not claim identical behavior for unrepresentable JSON numeric values, Swift Date's unbounded range, missing-reset versus negative-one sentinel deduplication, or changed public window identities.

## Native CI15/CI16 follow-up and open motion failure

- CI15 (`35877668146`, `ba30a32`) and CI16 (`35878689597`, `e7a0467`) passed Core/build/package and earlier x64 installed scenarios, then both **failed** provider-tooltip position animation. Direct UI and ARM64 stages were skipped. Neither overall run is PASS.
- CI15 receipts confirm grouped provider controls, drafts/alerts, reset-credit variants, raw Pro/Pro 5x quota visibility and only-quota fallback. Its refresh glyph is 14.482 by 16 rendered points inside a 28-by-28 button, with both endpoints inside the button. The previous exact square-size assertion was not a valid clipping test.
- CI16's completed account-limit receipt and inspected native dark capture confirm mounted parser metadata/order, remaining progress, disclosure/focus, clock aging and minimum-width states. Light/contrast captures and empty/loading/unavailable checks were produced. This is synthetic installed x64 execution, not real account, physical keyboard or all-DPI proof.
- Popup captures alone cannot establish screen motion. New diagnostics retain the existing movement assertion and animation implementation, while recording actual rendered provider identity, open/menu state, monotonic elapsed time, notch-local DIP anchors, screen-pixel anchors/targets/centers and DPI before switching and across six frames. The source popup must actually contain Codex before switching to Claude. Native diagnostic execution and a confirmed root-cause correction remain pending.


## Native CI17 motion timing evidence

- CI17 (`35900339766`, `9abcd36`) completed the x64 job successfully, including installed/direct native UI and CLI checks. Actual ARM64 passed its installed lifecycle/UI scenario, then failed direct native UI at the same tooltip position assertion. This run is FAIL overall.
- The new diagnostic proves that the direct ARM64 first ApplicationIdle sample arrived at **208 ms**, with the animated anchor already 188.9976 DIP against its 189-DIP destination; all later integer screen centers were 401 pixels. The installed ARM64 run sampled at 24 ms and recorded centers 323, 385, 400 and 401 pixels. Both started from the correct Codex center at 298 pixels. x64 installed/direct similarly recorded intermediate positions. These observations explain a late-sampling failure mode but do not by themselves rule out a first-render stall in the failing run.
- The native fixture now subscribes to CompositionTarget.Rendering before requesting the provider switch, collects lightweight actual screen positions and anchors, then unsubscribes in finally and writes its receipt even on failure. It requires an actual intermediate screen position and a settled destination within two pixels; a changed start/end alone cannot pass. The same frame must also have an intermediate DIP anchor, preventing a content-size change alone from passing. Diagnostic disk writes run after observation and preserve any original verification failure. The animation algorithm and duration are unchanged. Builds113/118 passed with zero warnings/errors. Native re-execution remains required.


## Twentieth change batch: companion display and restart cache

- The public companion `limits` now follows the frozen Mac account publisher: Pro removes five-hour quotas only when another quota remains, reset credits stay out of the quota list, multiple quota buckets include their reported display names, and non-visible account states publish no old windows. The plan and reading come from one current-login ownership check. Generic provider units/data are unchanged.
- An optional `cachedLimits` field keeps the full raw reading for the desktop restart path. The original account-scope comparison still gates restoration; snapshots from older versions without the field retain their original fallback. Filtering the public display no longer discards five-hour Usage data or reset credits from the restart cache. The CLI's explicit public projection does not emit the cache or account scope and remains read-only. CLI snapshots describe the last desktop publication, not a new authentication check.
- Both public and cached readings undergo the same structural validation. Output is bounded to the reader's eight-MiB limit before replacing the previous file, preventing a doubled cache payload from creating an unreadable snapshot. An oversized-payload regression preserves the prior bytes and readable quotas.
- Build114 caught two test analyzer violations; those assertions were corrected without disabling checks. Builds115–118 passed with zero warnings/errors. Core28 passed **1,863 tests, 33 Windows-only skips, zero failures**, including 20 new presentation/cache cases. Process-level CLI checks pass for visible quota projection, no fallback to retained account data, Unicode, original units and cross-account Claude session writes. Independent review caught a Usage-only additional-window filter affecting CLI quotas; the publisher now preserves those buckets regardless of the Usage preference. Native fixtures check that distinction, full cache restoration and rejection under another isolated login identity. Cleanup preserves the initial error while independently restoring preferences, synthetic authentication and snapshot bytes. Build120 validates the final WPF fixture; Windows execution remains pending.
- Mac preservation check15 confirms **389 of 389 reference files unchanged**. Independent review and native completion are tracked separately; this remains an unreleased parity branch.


## Twenty-first change batch: provider-specific freshness

- The frozen Mac sources use different clocks: Codex becomes stale at five minutes and does not invalidate a newly read quota merely because its reported reset passed; Claude remains fresh through fifteen minutes and tolerates up to five minutes of future clock skew, but invalidates passed resets. Windows previously applied the generic five-minute/reset rule to both.
- ProviderReading now implements those account-specific policies, so connectors, notch, provider rows and companion evaluation agree. The Limits UI uses that same function for its clock, freshness label and pace visibility. Explicit failed/stale states remain sticky until a real reading replaces them. Other providers retain their five-minute/reset policy. The existing Codex one-minute future-timestamp guard remains a documented conservative difference for abnormal timestamps; this is not a claim of byte-identical malformed-input behavior.
- Timestamp comparison now uses elapsed durations rather than adding to the supplied current date. Range-edge fixtures reproduced ArgumentOutOfRangeException before the change and now pass without overflow. The targeted before run had **eight failures and thirteen passes**; the after run passed **21/21**. Build123 passed with zero warnings/errors. Core29 passed **1,884 tests, 33 Windows-only skips, zero failures**.
- Independent source review found no blocker. Native fixtures inspect a fresh Codex reading with a passed reset and Claude cards at six/sixteen minutes, including pace visibility, while restoring both provider readings and preferences. Latest Windows execution remains pending; previous native runs do not verify this delta.


## Screen evidence cross-check

- During image inspection, the delivered previews appeared inconsistent. Independent byte comparison disproved a difference between the three files: CI17 x64, CI18 x64 and CI17 installed ARM64 account-history overview PNGs are all 980×728, 60,189 bytes, SHA-256 `f7879d82794e5ae8f50ea30add5ddac9c501896bd6f43e476aa95ff6e11610b1`. Independent inspection shows the full 179,456 total and complete overview. No product or WPF capture defect is established by those files.
- The history fixture now verifies the exact Today value and formatted text after navigation, rather than only requiring a value different from the synthetic server total. It also saves a separate screen-pixel rectangle plus active-window, DPI and metric-layout diagnostics, keeping the existing visual-tree capture. Desktop capture neither activates nor moves a window and must stay inside the virtual screen; occlusion and layered-window inclusion require separate inspection. This is additional evidence, not a claim of a screenshot bug fix or physical-input parity.
- Build124 passed with zero warnings/errors, and independent source review found no blocker. Native screen-pixel evidence remains pending.


## Native CI18–CI21 follow-up

- CI18 (`36142496592`, `fff75fd`) passed x64 and actual ARM64 jobs, including installed and direct native UI, MSI lifecycle and CLI checks. The rendering-frame receipts show intermediate screen positions together with intermediate notch-local anchors. ARM64 direct recorded 319/335/351/364 pixels on its way from 298 to 400; the installed run also records actual intermediate positions. This verifies the observer correction on those runs, not physical input on the user's PC.
- CI19 (`36143000472`, `17b4d72`) passed both architectures. Installed/direct ownership receipts on both architectures completed the companion-publication, raw restart-cache and changed-owner rejection fixtures. CI20 (`36143591151`, `ee64969`) also passed both architectures; all four account-limit receipts completed the Codex fresh-reset and Claude six/sixteen-minute freshness checks. The x64 Claude capture shows Updated 6 min ago with pace visible.
- CI21 (`36144504191`, `8585b72`) failed installed x64 at the new screen-capture assertion: the full requested window rectangle was outside the virtual screen. Direct UI and ARM64 did not run. This does not establish a missing Today value; the earlier exact-value assertion had passed. The capture helper now records requested/actual rectangles, fullWindowCaptured, monitor bounds, DPI and active state, and captures only their visible intersection. The Today rectangle must fit both the capture and one actual monitor, rejecting gaps between monitors. A partial capture is explicitly not whole-window evidence. Independent review found the original monitor-gap diagnostic weakness and confirmed this source correction. New native execution remains pending.

## Twenty-second change batch: provider account presentation

- The pinned script mapping now retains the response's optional accountEmail with its reading, existing plan and an actual api/web source. All sixteen script readers use this path; the presence of a label still depends on what the reader reports. Control characters and values over 256 UTF-8 bytes are omitted from display fields. No credential is used as a label or persisted in the metadata.
- Copilot displays GitHub as its source and uses a hosts username only when that entry's token equals the selected request token. Saved/environment credentials never borrow another CLI account's username. The existing post-request token check and store generation/scope guard remain in place. This is local token-bound display evidence, not server verification of the username.
- GLM preserves the source/region from the exact borrowed Claude Code/ZCode/OpenCode profile. Its management links follow the native Mac GLM adapter, not the different upstream generic Zai descriptor. Fixed global/China destinations update with the displayed reading. Other links come from fixed reference descriptors, never vendor response URLs or user credentials. The source labels and account actions of remaining native adapters still require implementation/comparison.
- Nonlocal details now place Signed in as, Plan, Credential from and Open usage page under the existing usage section. Optional label/plan rows and leading separators disappear when absent. Polling updates existing controls; changed/removed accounts clear fields without discarding credential drafts. Long values use a 240-point maximum, ellipsis and full-value tooltip. List summaries include the available identity/source from the same reading.
- Display metadata is stored only in optional owner-scoped restart data. Public companion limits and CLI output omit it; legacy snapshots remain readable. Independent review caught the initial public projection leak and unbounded Auto-column text before this change was committed. Both have source fixes, process tests and new mounted native assertions.
- Four targeted cases failed before the mapper change. The first extended run passed 28/29; its normal Perplexity response fixture omitted renewal_date_ts and therefore produced Error. Supplying that normal response field fixed the fixture without changing the script, and the next run passed 35/35. Build133 passed with zero warnings/errors. Core30 did not execute tests because its child apphost lacked DOTNET_ROOT; Core31 with the existing runtime correctly configured passed **1,919 tests, 33 Windows-only skips, zero failures**. After the Region additions, Core32 passed **1,923 tests, 33 Windows-only skips, zero failures** and the CLI process checks passed again. Native execution for this delta remains pending.
- CLI process checks pass, including six commands/formats that confirm identity metadata is not added to output and the input file is unchanged. New native assertions cover token-bound Copilot labels, GLM's actual source/region through connector/store/view, public-vs-cache metadata, long/missing account rows at minimum width, link destination and invalidation with retained drafts. They are pending execution and are not covered by CI20.

## Reference drift detected on September 25

- Preservation check16 found **383 of the original 389 hashes unchanged**. Six files in the separate Mac checkout now differ from the frozen hash baseline: LocalTokenUsage.swift, NotchLayout.swift, NotchUsageModel.swift, NotchViewModel.swift, TooltipCard.swift and Tests/CodeRimTests/Notch/LocalTokenUsageTests.swift. Their current filesystem modification times are September 25, 22:44–22:48 KST. This task's writes, commits and harness patches remain confined to Windows and this comparison document; those Mac changes are preserved, not reverted or adopted silently as a new baseline.
- This invalidates a present-tense claim that all 389 Mac files remain unchanged, while prior preservation receipts remain accurate for their check times. The changed reference paths need a separate read-only comparison before final parity acceptance. The baseline hash manifest has not been rewritten.


## Twenty-third change batch: local token state and scope

- **WP-27 / Medium.** Files: `Windows/src/CodeRim.Windows/Views/NotchPopover.cs` (`Create`, `LocalTokens`), `ViewModels/DashboardStore.cs` (`RefreshLocalAsync`), `DashboardStore.LocalData.cs` (`RunLocalDataOperationAsync`), and `CodeRim.Core/Services/LocalTokenPresentation.cs`. Before this change, `UsageSnapshot.Empty` rendered as `0 tokens (partial)`, Stale also rendered as partial, and a missing snapshot removed the local row. A failed repository import retained the previous quality unchanged. These are traced source paths; no pre-fix Windows execution for this delta is claimed.
- Local snapshot absence now means Loading; a completed unavailable/error read means Unavailable; measured zero stays 0 tokens. Partial remains a number, matching Mac, while Stale retains its explicit qualifier. Cached input is already part of the total and is not added twice. The Today label/value share one line, with `This PC · Since local midnight` and the corresponding timezone/account/session explanation below it. Fonts and block/scope spacing use the Mac design-frame measurements. Other providers without local transcripts do not receive this row.
- Each local reader now reports a failed scan/import as Error or Stale without replacing account readings or stopping subsequent local providers. A completed clear stores an empty completed snapshot, preventing a permanently loading row. Local source notifications continue through the existing DashboardStore property event and production NotchWindow popup update path.
- The reference is a separate read-only observation of the September 25 Mac local-token source, saved with hashes at `/private/tmp/coderim-mac-local-tokens-reference-20260925/reference.json`. This explicitly adopts its local-token behavior for Windows; it does not rewrite the original September 23 hash manifest, modify Mac, or resolve unrelated reference drift.
- Build134 caught a concrete-return-type analyzer violation; build135 caught a constant-array analyzer violation in the new fixture. Both were corrected without disabling analyzers. Build136 passed with zero warnings/errors. Twelve local token regressions pass, covering absence, completed-empty, measured zero, all quality states, cached input, locale/number style, retained failure data and calendar rollover. Independent review then found the empty-cache constructor produced Partial without an observed timestamp. CompletedRead now normalizes constructor and scan/import results without measurements to Unavailable, preserving measured zero and history. Both fixture paths now retain concurrent cleanup failures. Build137 passed with zero warnings/errors; Core34 passed **1,937 tests, 33 Windows-only skips, zero failures**, including fourteen local-token cases. New native execution remains pending.
- Native fixtures now exercise mounted loading/error/zero/partial/stale/long text cards, scope help, bounded text and absence on remote providers. A separate isolated JSONL/SQLite fixture uses the production popup and local reader for live changes, real database-open failure, recovery and clear, without fetching quotas. These assertions are added but not yet executed; the CI22 run at `4249cf9` does not contain this batch.


## Native CI22 evidence

- CI22 (`36147470331`, `4249cf9`) passed the complete x64 test/package job, including installed/direct UI and MSI lifecycle/rollback. The actual Windows ARM64 job also completed successfully, including installed/direct UI and CLI. All four receipts complete the account metadata, token-bound Copilot label, account-limit, local-data, settings, picker and ownership fixtures. This run includes provider account metadata and visible screen capture corrections, but not the later local-token changes.
- Both x64 desktop receipts locate the 980×728 window at (22,-4) on a 1024×768 display with a 1024×720 working area. The captured visible intersection is 980×724, explicitly `fullWindowCaptured:false`. Both exact Today value/text assertions passed (179,456), and the metric lies fully on-screen. Direct desktop pixel inspection confirms the complete numeric total. The default window exceeding the working area by eight pixels is a separate product placement issue; native layout fitting is next.


## Twenty-fourth change batch: settings window fits the display

- **WP-28 / Medium.** File: `Windows/src/CodeRim.Windows/Views/DashboardWindow.Shell.cs`, `ConfigureShell`/`FitSettingsFrame`. Reproduction: start the default 980×728 settings window on the CI22 1024×720 working area. Installed and direct receipts both report y=-4, with four pixels above and four below the working area. The earlier caption-only check accepted the frame because its caption point was still visible.
- On initial load, restore from maximization, monitor layout/work-area change, DPI change and completion of a move to another equal-DPI monitor, the normal window now bounds both its content size and position to the selected monitor's working area. Small/high-DPI screens can reduce the normal minimum to the available size; larger screens retain the 840×608 minimum. Position correction uses physical-pixel deltas at the actual HWND DPI, preserving negative/mixed-monitor origins. Maximized/minimized windows are left to the normal Windows state handling. Display events and the native move-completion hook unsubscribe on close. Same-monitor manual positioning is preserved; cross-monitor fitting waits until the drag completes.
- `NativeSmoke.WindowFrame.cs` adds isolated default/offscreen/oversized/restored frame cases with exact size/origin/DPI diagnostics and both visual-tree and desktop captures. It preserves the original saved frame and reports cleanup failures with the original failure. Build141 passes with zero warnings/errors after the native move-completion hook and keyboard/programmatic monitor-change additions. DPI/work-area fitting is deferred while a native move/resize loop is active, then applied after it ends. Core34 remains the latest successful Core run; this batch changes WPF only. Native execution, mixed-DPI physical transitions and taskbar movement remain unverified.

- Preservation check17 still reports the same six pre-existing Mac-reference differences; none of the five September 25 read-only local-token reference files changed after they were observed. This task continues to write only Windows and this comparison record.


## Native CI23 failure under investigation

- CI23 (`36149057421`, `180168d`) failed the installed x64 smoke at the new local-token Loading text assertion. Direct x64 and ARM64 UI did not run. Earlier local-data fixtures completed; this is not a complete regression pass. The failure occurred before the new state capture was written, so it does not yet distinguish a product text mismatch from a fixture's text-reading assumption.
- The fixture now records the expected text, TextBlock.Text, TextRange content, each Run, supplied/current snapshot quality, culture, dimensions and a visual-tree PNG before asserting. It keeps the exact assertion. The independent frame fixture runs before the token fixture so that a later token failure does not erase its evidence. No product text was changed on an unverified assumption. Build142 checks this diagnostics-only delta.


## CI25 diagnosis and twenty-fifth change batch: tooltip tail

- CI24 (`36149944740`, `44c626a`) repeats the installed x64 Loading assertion failure. CI25 (`36150371175`, `0c408c4`) records its actual cause: TextBlock.Text is empty, while both the TextRange and individual colored Runs contain exactly `Today  Loading…`. Direct inspection of the native PNG shows this same visible text. The fixture now reads the rendered text container for both the mounted state cases and live production-popup assertions. The expected text and state checks have not been relaxed; no product change was made to conceal this fixture failure. The remaining local-token cases have not yet run.
- Before that failure, CI25 completed all four installed x64 frame cases: default 980×720 at (22,0), restored offscreen/oversized/maximized frames 1024×720 at (0,0), within the 1024×720 working area. Each desktop capture reports fullWindowCaptured=true. This closes the reproduced default-frame clipping for that environment; physical multi-monitor transitions and ARM64 execution remain pending.
- **WP-29 / Low.** File: `Windows/src/CodeRim.Windows/Views/NotchPopover.cs`, `Tail`. The former straight 28-point triangle differs from the Mac TooltipTail's curved shoulders and 87-design-pixel cross-axis span. The Windows shape now uses the same two cubic curves, mirrored/rotated for all four edges, and retains its 75-design-pixel length. The native all-edge/all-scale fixture checks a closed two-cubic silhouette and its measured dimensions, with popup captures for visual comparison. Independent review found that WPF coalesces adjacent cubics into a PolyBezierSegment; the fixture now counts cubic triples in either representation rather than rejecting valid WPF geometry. Native execution for this tail is pending.


## Twenty-sixth change batch: provider tooltip content

- **WP-30 / Medium.** Files: `NotchPopover.cs`, `NotchPopover.Limits.cs`, `NotchMetrics.cs`, `CodeRim.Core/Domain/NotchUsagePace.cs`. Traced differences: the Windows title omitted Usage; the account row used different gaps and an extra email row; named quota groups had no outline; count-only windows consumed multiple lines; and pace used an unrelated five-point threshold and separate row. Mac source authority is the current read-only TooltipCard, NotchLayout and UsagePace implementations. The Mac adapter's actual headline selection was also traced and uses the tightest quota; its conflicting older comment was not treated as behavior and Windows headline selection remains unchanged.
- Windows now uses the reference glyph/title/body dimensions, 22-point plan/action row, switch-account icon/action, grouped outline/padding/gaps and single count-only table rows. Removing the extra tooltip email row does not change Settings, provider account rows or the separate account menu. Owner-scoped reading selection still happens before rendering. Inline summary checks for account isolation now read TextRange, retaining coverage after mixed-color Runs were introduced.
- The notch has its own reserved/deficit calculation matching Mac, independent of Account Limits' projected-exhaustion calculation. Unknown/invalid/expired windows have no pace; over-limit usage retains its reported summary while pace clamps spent fraction to one. Fractional and near-zero differences retain the reference precision with a fixed decimal point. Pace appears beside the quota summary, orange for deficit. Count formatting preferences and absent denominators remain respected. Summary width caps unscaled text before the 0.85 minimum scale, with ellipsis for longer content.
- Build146 caught a casing-comparison analyzer warning, corrected by checking Unicode uppercase/titlecase categories rather than changing the comparison to case-insensitive equality (which would alter its meaning). Build147 passed. The first targeted test run passed 13/18; five InlineData cases failed before reaching the test body because integer literals were supplied to nullable double parameters. Explicit double fixtures corrected that test defect; build148 passed with zero warnings/errors. Core35 passes **1,955 tests, 33 Windows-only skips, zero failures**, including all eighteen pace cases. The new native fixture remains pending.
- New mounted assertions cover account-switch navigation, no extra identity row, separated repeated groups, zero/over-limit bar dimensions, count-only rows, no unknown-denominator bar, inline pace/color and its disabled setting. Source review and native results remain separate; status/fidelity/freshness-footer comparisons are not closed by this change.

- Independent source review found that the initial title immediately truncated, unlike Mac's 0.85 minimum scaling. The header now scales a bounded single line before ellipsis; a long Moonshot catalogue-title fixture checks that behavior and its card bounds. This refinement changes WPF presentation only; Core35 remains the latest Core result.


## Native CI26 x64 evidence and twenty-seventh change batch: popup status

- CI26 (`36152023335`, `ff4001a`) completed the x64 test/package job successfully: installed/direct UI, MSI lifecycle and CLI. Both native receipts complete seven local-token states (Loading, Unavailable, Error, zero, Partial, Stale and long values), the actual JSONL/SQLite failure/recovery/clear sequence with zero quota requests, four settings-frame cases, and the all-edge/all-scale popup checks. Direct inspection of windows-popup-Right.png confirms the curved shoulder silhouette. MSI evidence completes install, injected-upgrade rollback, upgrade, downgrade rejection and uninstall preserving user data. ARM64 remains running at this record; the later popup-content batch is not part of CI26.
- Preservation check18 has the same six independently modified Mac paths as check16/17, with 383/389 original hashes unchanged. No additional Mac differences were found. This task's edits remain Windows-only plus this document.
- The popup now shows a neutral body-sized empty-state message with the reference block gap: waiting, sign-in guidance, connected-without-quota, unsupported reason or read failure. When retained quota windows exist, they remain the content instead of receiving an unrelated empty-state message. The existing switch-account action supplies the account route, so the extra Connect button is removed. The reference has no Updated footer in this notch card; its showLastUpdated preference belongs to the normal Usage surface, which remains unchanged.
- One intentional semantic exception remains explicit: Windows' distinct Disabled state keeps its Settings-off reason. Mac's current adapter maps disabled limits to needsAuth, which would direct an already signed-in user to sign in again. Mac source remains unchanged, and this difference is not counted as literal parity.
- Files: NotchPopover.cs, NotchPopover.Status.cs and NativeSmoke.NotchLimits.cs. Build151 passes with zero warnings/errors. The fixture now covers six empty states, neutral color, absence of the extra footer/action, and retained stale quota values. Source review and native execution for this status delta remain pending; Core35 remains the latest Core run because this delta changes only WPF code and its native fixture.


## CI26 completion, CI27 x64 and twenty-eighth change batch: reset locale

- CI26 completed successfully on both x64 and actual Windows ARM64. Both ARM64 host receipts report Arm64 OS/process architecture, .NET 10.0.12, Windows 10.0.26200. Installed/direct ARM64 receipts complete the seven local-token states, independent JSONL/SQLite refresh/failure/recovery/clear with no quota requests, and all four frame cases. This is CI-host evidence, not execution on the user's PC.
- CI27 (`36153410710`, `0d99fab`) completed x64 installed/direct UI, MSI lifecycle and CLI successfully. Both new notch-limit-group receipts complete the title/plan/action, separated groups, count-only/denominator, zero/over-limit bar, inline pace/color, disabled preference and long-title tests. Direct image inspection confirms grouped outline/padding, inline colored pace, curved tail, and bounded Moonshot title. ARM64 is still running at this record. CI28 (`36154191460`, `7cb0844`) separately contains the later empty-state/footer change.
- **WP-21 reset locale / Medium.** File: `Windows/src/CodeRim.Core/Domain/ResetCopy.cs`. The old distant-reset format hard-coded `MMM d`, reversing day/month order for many locales and dropping East Asian day markers. A second reproducible path threw ArgumentOutOfRangeException when the selected calendar could not represent a reported date (UmAlQura and DateTimeOffset.MaxValue).
- A standalone read-only Foundation DateFormatter probe executed the same localized `MMM d` template used by the Mac source for en-US, en-GB, de-DE, fr-FR, ko-KR, ja-JP, zh-CN and pl-PL. Evidence is `/private/tmp/coderim-reset-reference-jxa.txt`. The initial Swift probe could not run because the selected Xcode has an unaccepted license; no license was accepted or Mac configuration changed. The system Foundation/JXA probe succeeded.
- The pre-fix regressions reproduced nine failures out of ten (only en-US matched). Windows now uses its locale's month/day order and literals, abbreviates month/day format tokens without rewriting quoted or escaped literal text, and returns Reset time unavailable for an unsupported calendar date or malformed user date pattern. Near-term/relative rounding and timezone boundary rules are unchanged. The first post-fix run passed ten cases; two extra escaped/malformed-pattern cases were then added. Build153 passed with zero warnings/errors; Core36 passes **1,967 tests, 33 Windows-only skips, zero failures** (2,000 total), including all twelve locale/range cases. Independent review and Windows execution of this date delta remain pending.
- Further source tracing confirms that Mac ProviderSettingsView's analytics flags are shared AppStorage keys. Separate per-provider analytics preferences would introduce a new mismatch; they are not being added. The remaining Claude work concerns integration enable/link/detected-owner lifecycle, polling and Usage availability independently of notch visibility.

- Independent review found two custom-format boundary cases: percent consumes a single following token, and a single day/month token can be interpreted as a standard date format. Both were fixed and four focused regressions added. Build154 passes with zero warnings/errors; the updated date fixture passes 16/16. Independent rereview resolves RESET-LOCALE-01 without additional findings. Core36 predates these four extra tests; Windows execution remains pending.
- CI27 completed successfully on both x64 and ARM64. CI28 x64 installed/direct UI, MSI lifecycle and CLI also passed; both new notch receipts complete all six empty states, retained stale windows and absence of the extra freshness footer/action. CI28 ARM64 is still in progress.


## Twenty-ninth change batch: Claude lifecycle and Usage availability

- **WP-13 / WP-18 / Medium.** Actual Mac ClaudeIntegrationStore and ProviderSettingsView distinguish enabling an integration, detecting a CLI identity and explicitly linking that identity. Windows previously offered only a synchronous hook-install button, and used the notch list to control Usage selection, scanning, quota polling and local-provider settings access. The new Core ClaudeIntegration coordinator, Windows host, provider account controls and shared Dashboard read policy separate these responsibilities. Mac source is unchanged.
- Fresh Windows settings explicitly start Claude disabled. Legacy files retain a nullable migration marker: notch intent can enable detection, but a link is restored only when the current owned helper journal matches the installed command and the quota cache has the exact current owner. The CLI must still verify that owner before local Usage becomes available. Missing, malformed, oversized and foreign caches cannot approve an account. Saved credentials, source transcripts and SQLite history are preserved.
- Enable detects without linking; Add Account verifies and persists the current owner. Helper failure keeps local Usage available. Disable waits for pending helper work, uninstalls, persists off and selects Codex while keeping the linked owner for re-enable. Disconnect keeps the integration enabled while removing its link. Failed uninstall or settings writes do not claim success; rollback reinstalls only through the helper's exact-compare contract and does not overwrite a user's later status-line edit. Replacement of an existing unrelated status line still requires the existing explicit in-app confirmation.
- The host runs one independent 120-second auth check, separate from General refresh and notch visibility, and cancels on disposal. Concurrent refreshes join. The UI no longer performs helper filesystem work synchronously. Account switching waits for helper work and final verification is bound to the user's selected account ID; an external B-to-C change cannot approve C merely because the prior B switch succeeded.
- Usage now offers real local integrations: Codex always, and Claude when linked/available. Other provider quota pages remain accessible with their existing connectors. Hiding a local provider from the notch no longer removes Usage selection or its local scan/refresh. Temporarily unavailable selected Claude remains displayed with a settings route and is excluded from selectable popover rows. Ownership/availability changes invalidate focused content immediately. Explicit disable/disconnect switches back to Codex. Codex/Claude settings remain reachable without a notch ring. Analytics preferences remain global, matching the actual Mac AppStorage keys.
- Independent review identified three P2 boundaries before publication: legacy non-object/cache-size errors could abort startup; a subscriber disk failure could strand a mutation counter; and the first switch-completion API carried only a success boolean. The implementation now guards migration shape and size errors, completes in-memory invalidation despite companion write failure and always unwinds operation state, and carries the expected account ID through the final probe. Core regressions cover those boundaries plus owner changes during helper installation, dispose/cancellation, link/write/uninstall failures, refresh joining and late-install/disable ordering. Native fixtures exercise the actual settings controls, focused Usage/picker changes, 120-second host policy, legacy file evidence and an actual blocked companion path.
- Initial Core lifecycle checks passed 15/15. Build156 failed two UI analyzer checks; those were corrected and build157/158 passed. Build159 encountered an MSBuild child-node termination (MSB4166); its temporary diagnostic directory was no longer present when checked. Build160 found one analyzer warning in a new native fixture; corrected, and build161 passed with zero warnings/errors using a single build node. Core37 passes **1,990 tests, 33 Windows-only skips, zero failures** (2,023 total), including the additional date and lifecycle boundaries. Final native execution and independent rereview of this batch remain pending.
- CI28 (`36154191460`, `7cb0844`) is now fully successful on x64 and actual ARM64; all four installed/direct notch receipts include the six empty states and footer removal. CI29 (`36156412639`, `6add2d6`) passed x64 Core/UI/MSI/CLI; ARM64 remains in progress and Claude lifecycle is not in that run.
- Mac preservation check19 matches the same six external differences in check18; 383/389 original snapshot hashes are unchanged, with no additional changes in the original reference checkout.

- The follow-up review caught two additional integration boundaries: the existing account-display native fixture captured its Claude scanner roots before isolating CLAUDE_CONFIG_DIR, and the unavailable-selection fallback could remain as a hidden stale ComboBox item after selecting Codex. Fixture roots are now isolated before store construction, asynchronous local scans are joined and awaited during cleanup, and every selector change synchronizes the item list or restores a rejected selection. Closed-picker keyboard/type-search bypasses have dedicated native assertions. These checks remain pending native execution; they are not counted as completed behavior merely from source review.

- CI29 (`36156412639`, `6add2d6`) is now successful on both x64 and actual ARM64, including date regressions and installed/direct UI/MSI/CLI. The Claude batch remains separate. The empty Loading popup image from CI28 was directly inspected: the curved tail, Usage title, neutral message and local-scope rows are present without an Updated footer.
- Final UI review also found cached Ready quotas could conceal a helper installation error. The display now treats those retained quotas as stale and shows last-known/error status while keeping local Usage available. The native fixture covers both retained quota and no-cache error presentations. The closed-picker fixture sends the bubbling KeyDown route after an unhandled preview so it actually exercises base ComboBox selection code; it does not claim physical keyboard input. Build166 passes with zero warnings/errors.

- After the final Core/UI boundaries, Core38 again passes **1,990 tests, 33 Windows-only skips, zero failures** (2,023 total). It is the final local Core result for this batch; no native lifecycle success is inferred from it.


## Thirtieth change batch: shared calendar dates and native fixture correction

- **WP-21 date follow-up / Medium.** The same hard-coded month/day ordering remained in server History dates and timeline day labels. Reset-credit expiration separately replaced literal `MMMM` text without parsing a date pattern and could throw for an unsupported calendar. Those call sites now share the reviewed CalendarDateText formatter. Server DateOnly values are formatted directly without timezone conversion; actual timestamps are converted to local time first. Invalid calendar/pattern values receive an explicit unavailable label. Near reset timestamps keep a literal colon, matching the actual Mac ResetCopy rather than a custom Windows time separator.
- The extraction preserves the previously reviewed quote/escape/percent/single-token parser. Three additional Core cases cover DateOnly-style labels and a custom time separator. Build168 passed with zero warnings/errors; Core39 passed **1,993 tests, 33 Windows-only skips, zero failures** (2,026 total). Independent source review found no new defect. Four mounted native reset-credit cases cover en-GB, ko-KR, ja-JP and UmAlQura range fallback; they have not yet run on Windows.
- Full timeline parity remains open: the actual Mac selected-day detail uses an abbreviated full date, while Windows still uses its month/day label there. This shared date fix does not close that separate difference or all locale/font/DPI combinations.
- CI30 (`36158651811`, `7f71383`) passed Core/build/package and the initial MSI installation/rollback/upgrade/downgrade stages, then failed installed x64 UI at the old assertion `Provider name typing no longer selects Claude`. Direct UI and ARM64 were skipped. None of the new Claude lifecycle/migration fixtures ran; this run is FAIL. The actual Mac switcher is a button with a popover and has no closed-button name-typing selection. The corrected fixture requires closed TextInput to preserve Codex, then opens the real list with Space, moves with Down and confirms with Enter. It requires Claude in the picker, persisted Usage setting, closed popover and updated logo. Existing custom-search and unavailable-provider bypass checks remain. Build169 passed with zero warnings/errors; native re-execution is required.


## Thirty-first change batch: cost coverage and analytics charts

- **WP-31 / Medium.** Files: Core `UsageAnalytics.cs`, `AnalyticsTimeline.cs` and Windows `UsagePane.cs`/`UsagePane.Timeline.cs`. The actual Mac CostDisplaySummary excludes incomplete models from the range subtotal and passes that same exclusion set to each chart interval. Windows previously priced individual events independently, so complete records from an otherwise incomplete model could appear in earlier bars. It also treated a measured empty interval as missing cost and rejected output-only records merely because input-cache metadata was absent. Six new regressions reproduced those paths before the fix.
- Cost coverage now follows the model summary, including aggregated nullable cache-write metadata, and each interval uses the range's excluded models. Unknown/invalid pricing data remains unpriced; other complete models retain their explicitly partial subtotal. Actually empty measured intervals have zero cost, while intervals containing only excluded models are gaps. Input-free usage does not require input-cache metadata. The existing conservative unknown-high-context guard remains: Windows still lacks the Mac's explicit high-context import metadata, and this batch does not declare complete pricing parity. No price catalogue or persisted event/schema was changed.
- Cost-enabled charts use the reference 80-point frame and green bars; token-only charts retain 112 points. Partial coverage has the reference heading, a wholly unpriced range has an explanation instead of an empty chart, and selected details sit in the 10-point inset, 8-point rounded card. Existing month/day selected-date format, full axis/drag/keyboard behavior and complete metric-summary parity remain open.
- The intended pre-fix test filter was ignored by Microsoft.Testing.Platform (MTP0001), so that actual run executed the full suite: **six failures, 1,994 passes and 33 Windows-only skips**. Its UTF-16 detail log was decoded to a separate UTF-8 evidence file; all six failures are the new CostCoverageTests cases. The corrected targeted invocation (`-- --filter-class CodeRim.Core.Tests.CostCoverageTests`) then passed 7/7. Four more regressions cover aggregate metadata, ordinary requests summing beyond the single-request threshold, explicit exclusions on empty buckets and saturated excluded counts. Core40 passes **2,004 tests, 33 Windows-only skips, zero failures** (2,037 total).
- Build170 passed. Build171 caught a fixture constant-array analyzer error (CA1861); the receipt uses a list instead, without disabling analysis. Build172 passes with zero warnings/errors. The native analytics fixture now mounts an actual WPF Window and checks Today/7d/30d values, fractional bar ratio/common baseline, cost/full heights, theme color, zero/gap distinction, cross-day model exclusions, selected card and unavailable explanation. Independent review noted that a close error could skip later cleanup; restoration is now independent and preserves the initial failure. These newest native cases have not executed yet.
- CI31 (`36160068197`, `1d5ab91`) is checking the preceding shared-date/picker correction, not this cost delta. Mac preservation check20 exactly matches check19: the same six external differences, 383/389 original hashes unchanged, and no new Mac changes.


## CI31 completion and thirty-second change batch: Usage analytics hierarchy

- CI31 (`36160068197`, `1d5ab91`) is successful on x64 and actual Windows ARM64. Installed and direct receipts on both architectures complete Claude enable/detect/Add, helper failure, notch-independent Usage, external owner invalidation, closed-picker bypass prevention, disable/re-enable/disconnect and independent 120-second polling. Six migration cases and the locale/calendar reset-credit checks also complete. ARM receipts identify Arm64 OS/process architecture and .NET 10.0.12. These are synthetic native CI checks, not real provider login or the user's PC.
- CI32 (`36161085265`, `5df6fbc`) passes its x64 test/package job, including MSI lifecycle and installed/direct UI. Both analytics receipts complete range/selected totals, fractional-cost ratio/common baseline, compact/full height, green cost bars, measured-zero/gap coverage, cross-day exclusions and the unavailable message. The captured light-mode chart was directly inspected. ARM64 is running. That run contains the preceding cost batch, not the following UI changes.
- **WP-20 / Medium.** Windows Usage analytics retained a period dropdown plus refresh, a large inner Usage history title, repeated dark dividers, vertical metric blocks and filled model buttons. The read-only Mac AnalyticsViews reference uses Today/7D/30D segments, a two-column exact token/cost summary, 13-point chart headings and plain two-line model links. The Windows activity surface now follows those controls, margins and typography; selected intervals use the compact summary. Large exact numbers scale down within their column. An unknown model's pricing warning belongs in its detail, not every list row.
- Model details now retain the range inherited from their source, show the shared summary, Input/Cached input/Output and Projects/Sessions counts, and omit the unrelated period control and chart. Back restores the previous range and selection. The native fixture checks exact totals independent of compact-number preferences, model detail/Back and column bounds at 360/450/650 points. The layout check waits for dispatcher layout after navigation rather than asserting a newly created control's unarranged height.
- Files: `UsagePane.cs`, `UsagePane.Timeline.cs`, new `UsagePane.AnalyticsPresentation.cs`, `NativeSmoke.cs` and `NativeSmoke.Analytics.cs`. Build173 passed. Build174 caught a CA1859 return-type analyzer error; the helper now returns its concrete Grid without suppressing analysis. Build175 passes with zero warnings/errors. This batch changes WPF only; Core40 is still the applicable latest Core result. Independent source review found a missing minimum number scale. The summary now caps unscaled text to preserve the reference 0.65 minimum and truncates longer values. Mounted long.MaxValue cases check both the range and selected-interval glyph bounds and transform scale. Build176 passes with zero warnings/errors. A follow-up review also required both 28/18-point summary contexts to exist before the long-number loop, preventing a vacuous partial check; that guard is added and build177 passes with zero warnings/errors. The new native UI assertions remain pending. No additional product source blocker was found in the independent review.
- Empty/loading analytics snapshots, full selected-date/currency localization, chart axes/drag/keyboard fidelity, project/session surfaces and physical DPI/font comparison remain open; no whole-analytics or 100% match is claimed. Preservation check21 exactly matches check20: six separately changed Mac files, 383/389 baseline hashes unchanged, and no additional Mac changes from this task.


## Thirty-third change batch: analytics empty, loading and failure states

- CI32 (`36161085265`, `5df6fbc`) is now successful on x64 and actual Windows ARM64. Both ARM installed/direct analytics receipts complete the seven coverage/chart groups, and host receipts identify Arm64 OS/process architecture. CI33 (`36162819792`, `796ffbe`) is validating the subsequent range/summary/model UI; its results do not cover this newer state delta.
- **WP-20 / Medium.** Activity previously returned the same generic no-local-usage message for an empty selected range, while failed local refreshes could display retained analytics without the Mac's snapshot warning. Traced actual Mac `AnalyticsViews.analyticsPlaceholder/analyticsSnapshotStatus`, `SQLiteDatabase.analyticsSnapshot/databaseQuality` and `CostDisplaySummary`: an empty queried range has zero tokens, unavailable pricing and no model rows; failure is distinct from a completed query. A measured empty interval within a nonempty range remains zero cost, as in the preceding coverage fix.
- Windows activity now displays an initial loading indicator without totals, a dedicated first-read failure without cached-event values, or the retained snapshot with the reference warning. An empty range retains its zero token chart and no-model message while both total and selected-interval costs remain unavailable. Provider quota refresh does not override these local-history states. The separate overview/notch unavailable-versus-measured-zero policy is unchanged.
- New mounted native fixture covers loading/motion teardown, empty total and selected interval, data outside seven days returning in 30D, stale values/warning, first failure during a quota refresh, recovery and clearing. It independently restores the window, usage/events, refresh marker and cost setting, preserving a primary failure if cleanup also fails. These are new assertions awaiting native execution, not completed user-PC evidence.
- Files: `UsagePane.cs`, `UsagePane.Timeline.cs`, `UsagePane.AnalyticsPresentation.cs`, new `UsagePane.AnalyticsState.cs`, new `NativeSmoke.AnalyticsStates.cs` and the smoke entry point. Build178 passed; the warning symbol then changed from a platform glyph to a themed vector. Build179 passes with zero warnings/errors. Independent review identified four follow-up state boundaries, recorded and resolved below.
- This batch addresses activity and model surfaces; project/session presentation, full focus/scroll preservation, chart keyboard/drag, locale formatting and physical DPI comparison remain explicit work. No new provider credentials, network calls, persisted schema or Mac source changes are introduced.

- Independent review ASTATE-01/02/03/04 found: model details bypassed the new first-error guard; changing Partial to Stale discarded history completeness; a stored zero-valued model could still display $0 in an unavailable range; and RefreshReadings deferred state changes while a chart had keyboard focus. The shared activity/model guard, range-wide pricing quality and immediate state-change refresh now cover these paths. A small in-memory `RetainsPartialHistory` field preserves completeness through repeated failures; a completed reaggregation or clear resets it. Companion/CLI/SQLite use explicit projections and their schema is unchanged. The native fixture now drives focused RefreshReadings and checks model failure, partial-plus-stale and zero-valued model cases.
- Build180 passes with zero warnings/errors. **Core41 passes 2,005 tests, 33 Windows-only skips, zero failures (2,038 total)**, including repeated partial failure, recovery and clear. Independent rereview marks all four findings source resolved. Native state execution remains pending; a source fix is not native proof.

## CI33 x64 and capture-coordinate correction

- CI33 (`36162819792`, `796ffbe`) passes x64 Core/build/package, MSI lifecycle and installed/direct UI. Both analytics receipts complete the new three-segment range, exact summary, model detail/Back, 360/450/650 layout and large-number 0.65-scale assertions. ARM64 is running. These results precede the state changes above.
- Direct inspection of its `windows-analytics-cost.png` and `windows-analytics-width-360.png` exposed a **verification capture defect**: a child centered inside the fixture Window was shifted by its parent-relative visual offset and cropped at the right edge. Existing visual bounds assertions still measure the actual controls, but these offset child images cannot establish full visual parity. The 360 image's content started roughly 162 points into its own canvas.
- `NativeSmoke.Capture` now uses the child's actual visual offset as the source viewbox origin while keeping the output viewport at zero. A new mounted coordinate fixture checks exact red/blue edge pixels and image dimensions at both zero and nonzero offsets before other UI captures run. The earlier offscreen-content clipping fix remains intact. This changes verification capture code, not the installed app's layout.
- Build181 caught CA2219 in fixture cleanup; cleanup errors are now retained and rethrown after finally with the primary error. Builds182/183 passed with zero warnings/errors. Independent review required top/bottom samples too, so the fixture now checks four corners for both X/Y offsets. New pixel/native evidence is pending. Mac source remains read-only.

- A same-pattern search also found the overview's Today cost caption dropped partial-history disclosure after failure. It now reads the retained flag, as does the shared summary fallback. The native sequence returns from stale activity to Overview and then model detail to check all three surfaces. These are presentation-only follow-ups to the already passing Core41.
- Mac preservation check22 exactly matches check21: 383/389 original hashes unchanged and the same six separately modified paths, with no new Mac differences from this work.

- Build184 passes with zero warnings/errors. Final independent source review finds no open blocker in the state, partial-history or capture delta; the four-corner correction resolves CAPTURE-01. Corrected native images/pixel receipts and state execution remain pending.


## Thirty-fourth change batch: independent analytics navigation state

- CI33 (`36162819792`, `796ffbe`) is successful on x64 and actual ARM64. Both ARM installed/direct receipts complete the eleven analytics checks, including segment/summary, model Back, narrow geometry and numeric minimum scale. Its child PNG offset limitation remains as documented; CI34 (`36164801321`, `54dc99e`) is validating the corrected captures and analytics failure states.
- **WP-20 / Medium.** Actual Mac `MenuNavigation` owns independent Usage 7D, Projects 30D and Sessions 7D ranges and keeps them while popping/reopening a destination. Windows overview links always forced 7D, Projects used the wrong initial range, the external Sessions route forced All time, and leaving/reopening Usage discarded the selected interval. The new per-pane range state follows those reference defaults and keeps each list independent. The chart interval survives overview/other-list round trips and is cleared when the Usage range changes. Both direct and preference-driven provider changes clear those states, matching the provider identity boundary in the actual Mac settings view.
- Shared `ChangePeriod` updates only the active analytics list's saved range; changing an existing project/session detail's optional Windows period does not overwrite its parent list. The existing Back stack still restores detail-specific period/filter/model/bucket state. The additional Windows list search/period options and full project/session row/detail layouts remain explicit differences, not silently counted as complete parity.
- Build185 and build186 pass with zero warnings/errors. Core41 remains the latest applicable Core result; this delta changes only WPF navigation. Native assertions verify Projects' initial 30D, independent saved list ranges, interval restoration across another list and interval clearing on a Usage range change. Independent range and selected-interval source review finds no additional blocker. New native execution remains pending.
- A fresh read-only Codex task inventory returned no remote Windows task among the available fifty entries and no remote execution tool is exposed here. No unrelated task was moved or created. Native CI remains distinct from the user's physical Windows PC; no installed user app or public release is updated by these verification-branch commits.

- Builds187/188 pass with zero warnings/errors after strengthening the navigation fixture. It now compares the restored interval date and all displayed values, establishes a nondefault range and selected interval before a direct provider round trip, and separately exercises preference-driven provider switching. These assertions await Windows execution.

## CI34 native results and thirty-fifth change batch: Claude refresh completion race

- CI34 (`36164801321`, `54dc99e`) completed successfully on x64 and actual Windows ARM64: Core/build/package, MSI lifecycle, installed/direct UI and CLI. The x64 installed/direct receipts pass the four-corner offset capture checks and all seven analytics loading/empty/stale/error/recovery groups. Directly opened corrected `windows-analytics-width-360.png` and `windows-analytics-cost.png` show content within the full canvas; the old parent-offset clipping is absent. This run predates the independent-range navigation batch.
- **BUG-035 / Medium.** File `Windows/src/CodeRim.Core/Services/ClaudeIntegration.cs`, `RefreshAsync`/`RefreshCoreAsync`. CI35 (`36165785938`, `35a9b7d`) failed one Core test, `NotificationFailureCannotStrandBusyMutation`: 2,037 passed, one failed. The cause is the shared-task assignment, not the navigation delta. `Task.Yield()` can complete before the right-hand side returns; finally clears `refreshing`, then the caller assigns the already completed task back. Busy remains true and later refreshes can reuse that stale task indefinitely.
- A deterministic inline SynchronizationContext regression reproduced the stale Busy state before the product fix (18 passes, one new regression failure). The first local invocation did not start the test host because DOTNET_ROOT was omitted; the corrected invocation produced the recorded failure. The fix registers a TaskCompletionSource before scheduling and forwards success, fault and cancellation after normal cleanup. No sleeps or retries conceal the race.
- Independent review found that generic exception forwarding initially changed an external OperationCanceledException from Canceled to Faulted; explicit cancellation forwarding now preserves both task status and token. Added regressions cover synchronous completion/repeated refresh, observable notification failure/retry and notification cancellation/retry. Existing coalescing, late helper install, owner isolation and Dispose tests remain intact.
- Builds189/190/191 pass with zero warnings/errors. Final Core43 passes **2,008 tests, 33 Windows-only skips, zero failures (2,041 total)**. Windows-native execution of this fix and the preceding navigation batch remains pending; CI35's native jobs never ran. No Mac source, installed user app or public release was changed.

## Thirty-sixth change batch: chart frame includes its date axis

- CI34 ARM64 artifact receipts were also inspected directly: OS/process both Arm64, installed/direct four-corner captures and all seven analytics state groups completed. Its ARM64 narrow PNG was opened and shows the corrected capture origin. This is native CI evidence, not the user's physical PC.
- **WP-20 / Medium.** `UsagePane.Timeline.Chart` used the Mac 80/112 height for bars alone, then appended an extra date row plus vertical margins. The actual Mac `AnalyticsViews.metricChart` applies its height to the complete Chart, including the X axis. Windows now fits a date-axis row inside that same frame and sizes bars from the remaining plot area. The final date label no longer uses the bold summary-row style. Single-hour charts display one endpoint label.
- New native assertions check both axis bounds and regular caption typography, bar/axis separation, the ten-to-one cost ratio, all 360/450/650 widths and the token-only 112 height. Builds192/193 pass with zero warnings/errors. Core43 remains applicable because this batch changes only WPF rendering and its native fixture. New native frame execution is pending; adaptive Swift Charts tick choices, locale date details and pointer/keyboard fidelity are still not claimed identical.
- Independent review CHART-FRAME-01 found a vacuous narrow-axis loop and an overly broad receipt. The fixture now requires both token/cost axes at every tested width and checks actual rendered fill bounds against the axis; the token-only path also checks every bar. Build194 passes with zero warnings/errors. The 18-point axis allocation is the Windows layout policy; full native Swift Charts tick typography is still outside this claim.

## Thirty-seventh change batch: project/session failure boundaries

- **BUG-037 / Medium.** `UsagePane.Detail` applied the analytics readiness check only to activity/model. Project/session lists and details continued consuming retained Events even when no usable local snapshot existed, and stale rows had no warning. The guard now runs before filtering or rendering every analytics destination; first failures use the matching Projects/Sessions title. No database values, account ownership rules or period/Back state change.
- Native fixture additions navigate both real list routes with retained events and missing/failed Usage snapshots, require numeric rows to stay hidden, recover the list, open detail, inject missing/failed detail snapshots, and then verify retained values plus stale disclosure in detail and after Back. Builds195/196 pass with zero warnings/errors. These new assertions have not yet run on Windows.
- Independent comparison distinguishes the actual Mac list's `AnalyticsDetailLayout` loading/placeholder/stale behavior from its project/session detail, which directly conditions on snapshot existence and has no equivalent explicit status helper. Windows' explicit detail failure disclosure is a safety/clarity fix, not a claim of pixel-identical detail status. Full project/session presentation and per-range snapshot architecture remain tracked differences.
- Preservation check23 exactly matches check22: 383/389 baseline hashes unchanged and the same six separately modified Mac paths, with no new Mac delta. No Mac app or source was changed by these batches.

## Native navigation/frame results and thirty-eighth batch: analytics list ordering and names

- CI36 (`36166921498`, `51f8b91`) passes the x64 Core/package/MSI lifecycle and installed/direct UI/CLI. Both downloaded analytics-state receipts complete nine groups, including independent saved ranges, exact selected-interval restoration and direct/preference-driven provider resets. ARM64 is still running at this record.
- CI37 (`36167443567`, `b79911f`) completed the x64 installed/direct native checks. Both analytics-cost receipts complete twelve groups, including actual bar/axis bounds at 360/450/650 widths and the token-only 112 frame. The new narrow PNG was opened directly: date labels are regular captions inside the compact chart frame and no longer add the former extra rows. Final workflow/ARM64 status remains pending. These results precede the project/session state delta in CI38 (`36167950395`, `6b2d80b`).
- **BUG-038 / Medium.** Actual Mac `AnalyticsModels` orders models by total then identity, projects by total/name/identity and sessions by latest activity then identity. Windows model/project ties inherited input order, sessions sorted by total usage, and session rows displayed full opaque IDs. Windows now applies the reference ordering, names sessions from their project (or Session plus the first eight ID characters), and assigns each row a stable automation ID independent of its title. Name and full-ID search both remain available.
- The new Core model-order regression fails against the old sort (expected a-model before z-model) and passes after the tie-breaker. Build197 passes; Core44 passes **2,009 tests, 33 Windows-only skips, zero failures (2,042 total)**. The native fixture checks duplicate project names, equal activity times, recent low-usage sessions, input reversal, missing project metadata and an actual folder named Unknown project.
- Independent review AORDER-01 found that choosing each group's first event could change project/session names when metadata differed inside the same group. Naming now prefers known project metadata, then latest occurrence, with stable event/project/name tie-breakers. Added same-session/same-project mixed-metadata reversal and full-ID search assertions. Build198 passes with zero warnings/errors; Core is unchanged from Core44. The Windows selection is derived from events in the chosen range; it does not yet reproduce Mac's separately persisted global session_metadata naming in every history boundary. Full row/detail layout and metadata parity remain tracked work. Native execution of this newest batch is pending.


## Native completion and thirty-ninth batch: reference project/session lists

- CI36 (`36166921498`, `51f8b91`), CI37 (`36167443567`, `b79911f`) and CI38 (`36167950395`, `6b2d80b`) are now successful on x64 and actual Windows ARM64. Downloaded ARM host receipts report OS/process Arm64 and .NET 10.0.12. Both CI37 installed/direct cost receipts complete twelve groups; both CI38 state receipts complete ten groups, including Projects/Sessions list/detail loading, first error, recovery and retained snapshots. These results do not include the subsequent ordering or list-presentation changes.
- CI39 (`36169023755`, `151c5c0`) failed its installed x64 smoke before analytics. MSI initial install, rollback, upgrade and downgrade rejection had completed; a `BoundedProcess` command in the Claude hook stage timed out. The old stack lacks the specific command, so it does not establish whether PowerShell status input, connect or disconnect stalled. The failure is retained; no analytics success or ARM completion is inferred. The native fixture now records only operation stage/state/duration/timeout around those three commands and adds stage context to exceptions, with unchanged timeouts and no argument/input/output logging. Product process behavior is not changed on this incomplete evidence.
- **WP-20 / Medium.** Actual Mac Projects/Sessions use a full-width Today/7D/30D control and plain two-line rows. Windows had a second large title, permanent search, six-option period dropdown and filled buttons with abbreviated totals. Windows now follows the reference list hierarchy, horizontal16/vertical10 row padding, exact token totals, project session counts, optional estimates and preference-controlled agent/image metadata. Detail routes inherit their parent range. Mac source remains read-only.
- Existing Windows Find is preserved behind Ctrl+F, with name/full-ID filtering, an explicit no-match state and Escape clearing/hiding the field and returning range focus. Back restores both query and field visibility; forwarding to another destination resets both. This optional convenience is explicitly separate from literal Mac interaction parity. The existing History week/month/lifetime routes and list pagination remain available.
- Shared model/project/session row rendering keeps ellipsis and full tooltip/accessibility text. Native checks cover default controls, exact row padding, counts, enabled/disabled cost and metadata, 360/450/650 horizontal text bounds, inherited detail ranges, Back and optional Find empty/Escape/focus behavior. These are synthetic fixture widths and values, not all locales, long metadata or physical DPI states. Complete detail body, global persisted session metadata, abbreviated date formatting and paging remain open differences.
- Builds199/200 pass with zero warnings/errors. The final diagnostics/cost assertion build and independent rereview are pending at this record. Core44 remains applicable (2,009 passes, 33 Windows-only skips, zero failures), since this batch changes WPF presentation/fixtures only. New native list execution has not run yet; no public release or installed user PC app is updated.

- The extra positive cost check triggered CA1847 in build201; it now uses the required single-character overload. Build202 passes with zero warnings/errors. Independent review LIST-UI-DIAG-01 found that failure-receipt I/O could obscure the original subprocess exception; both errors are now preserved inside the stage wrapper. These diagnostics neither retry nor weaken a failing test. Final native execution remains pending.


## Fortieth batch: project/session detail hierarchy

- **WP-20 / Medium.** Files `UsagePane.cs` and new `UsagePane.EntityDetails.cs`. Actual Mac ProjectDetailView/SessionDetailView use a name with an exact 28-point token total, a small cost caption, per-entity metadata, Input/Cached input/Output and plain model totals. Windows showed a generic large title, large cost block and extra activity/cost charts in these details. The Windows-only delta matches the reference hierarchy, 16-point content inset, 14-point sections and 0.75 minimum scale on the token header. Overview, Usage charts, History routes and model-detail navigation remain intact.
- Project details show their distinct session count. Session details add Last activity; optional direct children use stable IDs, known project names, separate exact token totals and latest-activity ordering. Children outside the inherited range remain excluded. Whole-session image counts keep their semantics and explanatory tooltip. The list and detail now share the same stable event-derived naming choice. No database/schema or local import contract changes are included.
- Costs respect the range quality, provider capability and preference, show unavailable when unpriced, and retain the explicit partial-history and excluded-token/model caption. The existing stale/first-read guard still precedes all entity data. The explicit unavailable image count when metadata is absent is retained instead of fabricating zero. Last activity still uses Windows locale g; Mac abbreviated dates and separately persisted Started/global session metadata remain open.
- A new mounted native fixture covers own totals, cost coverage, model isolation, inherited range without extra charts/picker, direct-child names/totals/filter/Back, metadata preferences, unknown pricing and horizontal text bounds at 360/450/650. Existing main native assertions use stable sub-agent IDs rather than the removed opaque-ID button caption. The fixture uses synthetic data and cannot establish physical DPI, all locales or long-content pixel parity.
- Build203 caught a fixture helper/type naming conflict and five CA1861 constant-array diagnostics. The helper was renamed and expected content uses exact list patterns; build204 passes with zero warnings/errors. Core44 remains applicable because production Core is unchanged. Independent source review and native execution of this batch are pending. CI40 (`36171140076`, `b191b2c`) is running the preceding list UI/Claude diagnostic batch, not these detail changes.

- Independent source review found a P3 fixture gap: several metadata/breakdown assertions checked labels without pairing their values. The fixture now verifies exact Sessions, Input, Cached input, Output, Last activity, direct-child and image values in their own rows. It also checks long.MaxValue header bounds and the 0.75 minimum scale at width360. Build205 passes with zero warnings/errors; these strengthened assertions remain pending native execution.


## CI40 x64 native list results

- CI40 (`36171140076`, `b191b2c`) completes its x64 job successfully: Core/build/packages, MSI lifecycle and installed/direct native UI plus CLI. Both downloaded analytics-state receipts complete fifteen groups, including stable ordering/mixed metadata, new reference list controls/rows, counts/cost preferences, narrow text bounds and optional Find empty/Escape/focus behavior. MSI lifecycle completes all seven recorded checks. ARM64 remains in progress at this record.
- The 360-DIP session PNG was opened directly and the 450/installed360 images were independently inspected. The checked captures show the three segments, plain aligned rows and secondary metadata/cost within the canvas, without a newly observed clip or blank surface. This evidence is synthetic native x64 and does not cover all content/locales, physical keyboard/DPI or the later EntityDetail batch.
- The new command receipts finish at disconnect-restore-statusline (direct190ms, installed194ms); reaching this point also passed the preceding status stdin and connect assertions. The earlier CI39 timeout did not reproduce. Its exact root cause remains undetermined; operation diagnostics were added without retry or relaxed deadlines.
- Final independent source rereview resolves ENTITY-DETAIL-01 after exact metadata/breakdown value and large-header checks. Build205 is the final local detail build. CI41 (`36172052038`, `30a42a3`) is validating that source separately. Mac preservation check24 exactly matches check23: the same six external differences and no additional change among 389 locked files.


## CI41 completion and forty-first batch: localized analytics dates

- CI40 (`36171140076`, `b191b2c`) and CI41 (`36172052038`, `30a42a3`) are fully successful on x64 and actual Windows ARM64. CI40 ARM installed/direct state receipts complete fifteen groups. CI41 x64 installed/direct detail receipts complete nine groups and state receipts retain all fifteen. Primary and independent review directly opened the 360-width session detail and project detail PNGs; no clipping, overlap or incorrect fixture values were observed in those samples. CI41 ARM artifacts are being retrieved separately. Neither run includes the date delta below.
- **WP-32 / Medium.** Windows session list/detail used numeric g timestamps; selected chart dates omitted the year and Today selection forced a 24-hour clock with offset. Actual Mac AnalyticsViews uses abbreviated dates and shortened times. New Windows `AnalyticsDateText` uses medium-date/short-time styles from system ICU for those three call sites. Axis tick formatting is unchanged and remains a separate comparison item.
- A read-only Foundation DateFormatter probe at UTC 2026-10-09 15:04:05 produced date/date-time reference strings for en-US, en-GB, de-DE, fr-FR, ko-KR, ja-JP, zh-CN and pl-PL. A separate macOS system ICU C-API probe produced identical strings for all eight, including joining words and narrow nonbreaking spaces. Evidence: `/private/tmp/coderim-analytics-date-reference.json` and `/private/tmp/coderim-icu-date-probe.jsonl`. This is Foundation/system-ICU evidence, not a SwiftUI render or proof that every Windows ICU data version is identical. No Mac settings, source, app or Xcode license changed.
- The [Windows ICU documentation](https://learn.microsoft.com/en-us/windows/win32/intl/international-components-for-unicode--icu-) identifies the OS-provided combined DLL; the [Unicode C API reference](https://unicode-org.github.io/icu-docs/apidoc/released/icu4c/udat_8h.html) defines styles and buffer/status contracts. The wrapper loads only System32, explicitly encodes the locale, bounds UTF-16 output to 256 code units, frees each native formatter in finally and uses the offset at the actual instant. No NuGet dependency or downloaded DLL is added. Missing APIs and formatting errors use safe locale fallback.
- Independent review identified loss of explicit Windows 24-hour/date/calendar overrides when only a locale name was passed. Relevant pattern/separator/designator/calendar overrides, including Hijri adjustment, now retain the previous safe .NET formatting. Native fixture additions cover eight exact reference locales, localized Today time, day/year rollover, DST offset changes, unsupported fallback calendar dates and explicit time/date/calendar preferences. A receipt identifies whether the runner's current culture used native formatting.
- Build206 rejected the initial locale string marshal annotation with CA2101. The boundary now passes explicitly UTF-8-encoded, NUL-terminated bytes; builds207/208 pass with zero warnings/errors. Core44 remains applicable because only Windows UI/service/fixtures changed. New Windows locale execution and final independent rereview remain pending; no percentage or public release completion is inferred.

- CI41 ARM64 artifacts have now been read directly: both installed/direct detail receipts complete nine groups, and both state receipts complete fifteen. Final independent date source review resolves ADATE-01 after override preservation and confirms build208; Windows ICU execution remains pending. Harness worker55 records PASS with no scope violations, which is patch/isolation evidence and not formal harness acceptance.


## Forty-second batch: durable session names and Started

- **WP-33 / Medium.** UsageScanner already parsed Codex session metadata timestamps but dropped them from SessionDetails; UsageRepository stored links/images only. Session list/detail naming was derived from the selected range. Actual Mac SessionDetailView reads its Started row and name from session_metadata joined to ranged usage. Windows now carries the initial Codex timestamp and latest real context folder name into a new provider-keyed session_metadata table, and uses those values in the list/detail/sub-agent links independent of the selected range. Missing starts remain hidden. A dictionary built once per analytics render avoids a metadata list scan for every session name.
- This is an additive schema initialization, not destructive recreation. Existing links/images/tokens remain readable with absent new fields until the next scan. Metadata writes share the existing event transaction; rebuild/clear delete the same provider's metadata and rollback retains old metadata after failed enumeration. Codex preserves its first known start. Claude derives its earliest retained usage and latest named project from indexed retained events, so excluded records and the local-history cutoff cannot resurrect an old start/name. Known project metadata is not erased by missing data or stale copied context; equal-time names use a deterministic tie.
- The scanner now requires an actual turn_context or first session_meta record before changing model/project metadata. A payload containing that word in unrelated text and replayed later session_meta records cannot rename the session. Only the existing sanitized folder label and opaque identities are persisted; no complete paths, prompts or attachment contents are added.
- Read-only comparison found two additional distinctions: the current Mac CodexAnalyticsNames layer can overlay desktop thread titles in memory; its Claude collector also assigns separate identities to agent transcripts. Those are not implemented by this metadata batch or silently counted as parity. Claude Started currently means first retained assistant usage, consistent with the reference collector for ordinary sessions, not an invented process launch time.
- Seven new Core regression cases cover scan/reload, old-schema compatibility, stale/null/copy metadata, provider isolation, clear/rebuild/rollback, Claude cutoff behavior and no private full-path persistence. Builds209/210 failed two nullable test dereferences; the assertions now use the previously checked nullable value safely. Build211 passes with zero warnings/errors and the seven focused tests pass. The full Core45 run, independent review and the new mounted global-name/Started/range fixture remain pending. CI42 (36174652538, 5d5dd27) is validating the preceding date batch only.

- Core45 completed: 2,016 passes, 33 Windows-only skips, zero failures, 2,049 total. Independent source review found no new blocker in this metadata batch and confirmed provider isolation, migration/transaction/cutoff and UI dictionary paths. Native metadata display remains pending. CI42 completed with a genuine date-format failure in installed x64: system ICU returned `Oct 9, 2026, 3:04 PM` instead of the Mac reference joining word and narrow space. The expected strings are not relaxed; a separate explicit-pattern correction is required. ARM64 did not execute in this failed run.


## Forty-third batch: explicit Mac date patterns after native mismatch

- **WP-32 / Medium, reproduced on installed Windows x64.** CI42 (36174652538, 5d5dd27) failed AnalyticsDateRegression at en-US. Day matched; date-time was `Oct 9, 2026, 3:04 PM`, while Foundation requires `Oct 9, 2026 at 3:04\u202fPM`. Same style enum/API was insufficient because the OS ICU/CLDR defaults differ. The downloaded exception is retained in `/private/tmp/coderim-mac-parity-ci42-ui/installed/windows-installed-dashboard.error.txt`. Earlier installed analytics details/state/cost fixtures completed before this assertion, but direct UI and ARM64 never ran.
- New `Windows/Scripts/export-analytics-date-patterns.js` reads Foundation NSDateFormatter patterns for the reference Mac's available locales plus the explicit eight comparison aliases. It changes no system preferences or application files. The embedded JSON contains 1,067 locale keys, three patterns each (59,355 bytes, longest45 UTF-16-independent characters in this snapshot), with source OS provenance. The exporter rejects contradictory normalized aliases. Resource validation found no missing/empty/overlong/NUL patterns. Having a pattern is not evidence that every locale was rendered on Windows.
- For a known locale the existing system ICU formatter now uses explicit UDAT_PATTERN (-2) and the captured UTF-16 pattern, preserving reference joining words/hour cycle/spacing without manually composing translations. The [official ICU API](https://unicode-org.github.io/icu-docs/apidoc/released/icu4c/udat_8h.html) documents this style. Existing System32 lookup, bounded output, per-call handle cleanup, instant offset and user override fallback remain. Unlisted locales still use OS styles; localized symbol/calendar differences between ICU data versions remain unverified. No external DLL or package is added.
- Build212 passes with zero warnings/errors. The read-only Mac system-ICU explicit-pattern probe reproduces all eight independently captured Foundation reference strings (`/private/tmp/coderim-mac-pattern-probe.json`). Native expected strings are unchanged and now additionally require a captured pattern for each of those locales. Receipt adds the current culture's pattern. Core45 remains applicable because this follow-up changes WPF formatting/resource/fixture only. Independent source review and Windows native execution are pending.
- Preservation check25 exactly matches check24: 383/389 locked hashes unchanged and the same six external prior differences. This work did not modify Mac source, settings, app or license state. No installed user PC app or public release was updated.

- Independent review noted common Windows zh-TW/zh-HK/zh-MO/zh-SG names were not in Foundation's enumerated script-qualified list. The exporter now queries those aliases explicitly; a separate read-only Foundation probe supplies four additional fixed expected strings, expanding the native comparison to twelve locales without changing the original eight. Generic missing-locale fallback remains explicit.

- Build213 passes with zero warnings/errors after adding the four explicit Chinese locale aliases. The original eight expected reference strings remain unchanged.


## Forty-fourth batch: Claude sub-agent accounting and detail navigation

- **BUG-044 / Medium.** ClaudeJsonlParser/UsageScanner ignored agentId and agent-prefixed transcript filenames. A parent100 + child50 fixture yielded one session instead of two, while its total150 remained correct. The pre-fix regression fails Expected2/Actual1 (`/private/tmp/coderim-mac-parity-claude-agents-before-detail.log`, build214). Actual Mac processClaudeLine and testSubagentsAreIncludedOnceWithParentMetadata establish separate child identity and parent metadata. Windows now normalizes prefixed/unprefixed agent identifiers, scopes each child to its parent, preserves ordinary parent identities, and records durable parent links. Imported ownership evidence is excluded from serialized numeric history.
- Copied parent history cannot take a recognized child's message back. The merge retains maxima of disjoint token components and the earliest timestamp; only a recognized child of that exact previous parent can promote an existing collapsed row. Reimport updates retained parent and child metadata, even when the parent transcript was not scanned in that pass. Claude metadata is recomputed from all retained indexed events after cutoffs/exclusions so moving an old child cannot leave its start/name on the parent. Total accounting and Codex imports are unchanged.
- Independent review found **SEC-044 / Medium (CLAUDE-AGENT-01)**: loose parent IDs could contain the delimiter used in a derived child identity. Parent session IDs now use the Mac-compatible ASCII identifier alphabet and length bound; malformed parents cannot alias children. It also found **BUG-045 / Medium (CLAUDE-AGENT-02)**: initial ownership migration moved the session but retained old project/model fields. The exact promotion branch now moves authoritative project/projectId/model too. Both new regressions actually failed before correction (build217, 9 passes/2 failures), including a fresh-vs-migrated database comparison, and the failures are retained. No unrelated session can be reassigned by a colliding message ID.
- Builds215/216 passed the initial implementation and native fixture. Initial focused tests passed10 and Core46 passed2,026, but that was insufficient for the two independent findings. Final build218 passes with zero warnings/errors; Core47 passes **2,027 tests, 33 Windows-only skips, zero failures, 2,060 total**, including eleven Claude agent cases. Tests cover filename/field normalization, explicit-field priority, parent scope, malformed IDs, copied history in both orders, durable upgrade/restart/clear and provider accounting.
- New NativeSmoke.ClaudeAgents uses isolated synthetic JSON transcripts, the production scanner/SQLite and a mounted Claude UsagePane. It checks total150, parent100/child50, child-link navigation/Back, unsupported cost/image fields and agent-detail preference boundaries, then restores the store/settings and deletes the fixture. Its native execution remains pending. CI43 (36176597030, ce7f634) is validating the earlier session metadata and explicit date-pattern batches, not this new agent delta. Final independent rereview remains pending.

- Final independent Claude-agent source rereview confirms both P2 findings resolved and directly verifies build218/Core47 logs. No additional source blocker was identified in the reviewed delta; native fixtures remain pending.


## Forty-fifth batch: Codex desktop titles in analytics

- CI43 (`36176597030`, `ce7f634`) is fully successful on x64 and actual Windows ARM64. Downloaded installed/direct receipts on both architectures complete date17, details10, states15 and cost12 groups. The installed 360-width detail shows the original Started value and Mac date joining word without observed clipping. CI44 (`36177773100`, `51d54fb`) x64 is successful: installed/direct Claude agent receipts complete four groups; primary directly opened parent100/child50 screenshots. ARM64 for CI44 remains pending at this recording. These are synthetic Windows runner checks, not a user-PC installation or all-account validation.
- **WP-34 / Medium.** Mac CodexUsageCollector.analyticsSnapshot applies CodexAnalyticsNames using local state and desktop task metadata, including archived tasks. Windows previously showed only imported folder labels. New CodexActivityCatalogue.ReadAnalyticsLabels matches existing opaque session hashes, reads explicit name before local desktop title before stored title, and keeps optional labels in memory. Session lists, detail headers and child links use the same resolved title. Generic project names use the reference sorted first-two names and (+N) suffix. Missing labels preserve durable history names and numeric data; no schema change, title persistence or network call is added.
- Source databases remain read-only. Existing reparse/size/schema checks reject unsafe sources and generated identity fields; SQL execution and text/row allocations are bounded. Remote-host catalog entries cannot rename local sessions. Regular refresh and rebuild reload labels off the UI thread; clear removes them. Limits (50,000 state rows, 8,192 optional project/desktop lookups, SQLite work/time budget) can intentionally leave fallback names on very large or unsafe sources. Those fallback boundaries are not counted as universal title parity.
- Six Core cases cover archived/name/desktop precedence, saved/generic/projectless names, remote exclusion, missing optional stores, rejected labels/schema/links, cancellation and byte-for-byte source DB preservation. Build220 passed but four tests failed because the new fixture used macOS /var's symlinked temporary path; the existing security rejection was correct. The fixture now uses /private/tmp like existing protected-DB tests. Build221 and the focused six pass. Build222 caught an inaccessible internal hash helper in the new native fixture; its expected IDs now use public BCL SHA256 without widening the Core API. Build223 caught a constant-array analyzer issue; the fixture now uses a static readonly array. Final build224 passes with zero warnings/errors, and Core48 passes **2,033 tests, 33 Windows-only skips, zero failures, 2,066 total**.
- NativeSmoke.AnalyticsLabels connects real isolated state/desktop SQLite fixtures to mounted session/project/detail views, verifies exact totals, child navigation/Back, missing-source fallback and unchanged source bytes. Existing NativeSmoke.LocalData now tests task labels through production rebuild, normal refresh after rename and clear. These new native assertions and final independent rereview remain pending. No Mac source/settings/application or public release is changed.

- Final independent title source review finds no new blocker and confirms build224/Core48, Mac title precedence, read-only limits, provider boundaries and fixture cleanup. It separately inspected CI43 ARM date/details and CI44 x64 Claude images/receipts. These earlier artifacts do not validate the new title delta. Harness worker60/batch59 passes with no scope violations; the formal adapter verdict remains INCONCLUSIVE.


## Forty-sixth batch: request-aware high-context pricing

- CI44 (`36177773100`, `51d54fb`) is fully successful on x64 and actual ARM64. Downloaded ARM installed/direct Claude agent receipts each complete all four groups, matching the already reviewed x64 parent100/child50 evidence. CI45 (`36179678176`, `4b37dd5`) is running the title delta, not the pricing changes below.
- **WP-35 / Medium.** Windows excluded every usage event above 272,000 input tokens for tiered models. Mac CodexUsageCollector.pricingContext distinguishes a single known request (normalized delta equals lastUsage) from an ambiguous cumulative increment, and CostEstimator applies separate input/output multipliers. A new production scanner→SQLite→estimator regression reproduces the Windows omission: expected1.703, actualnull (build226 and `/private/tmp/coderim-mac-parity-high-context-before-detail.log`). Build225 first caught a constant-array fixture analyzer issue; that compile failure is not the reproduction evidence.
- Windows now carries optional Standard/HighContext metadata from normalized Codex requests, stores it in an additive nullable checked events.highContext column, and restores it after restart. Migration runs transactionally and preserves existing rows. Legacy small requests retain ordinary pricing; legacy large records stay unpriced until a proven source reimport supplies context. Missing metadata cannot erase a previously known context, and provider isolation, clear exclusions and rebuild transaction behavior remain. The metadata is excluded from exported event JSON; no prompt, full path or credential is added.
- Cost estimation separates known standard/high-context totals and applies the reference input multiplier to uncached, cached and cache-write input and the output multiplier to output. Ambiguous large increments still exclude their model throughout the selected range and chart. Ordinary requests whose sum exceeds the threshold remain standard. Untiered model rates are unchanged. No pricing catalog/rates/dependencies or Mac source are edited.
- Eight focused cases pass after the fix: boundary272000/272001, mixed requests and counter restart, unknown increment/model-wide chart gap, old database upgrade/reimport/reopen, rollback/rebuild/clear/provider isolation, exported JSON privacy, invalid context and untiered rates. Build229 passes with zero warnings/errors; Core49 completes **2,041 passes, 33 Windows-only skips, zero failures, 2,074 total**. The mounted cost fixture now checks a known request's ~$1.70 summary/nonzero bar and an unknown request's gap. Independent review and native execution of this new delta remain pending.
- Preservation check26 exactly matches check25: 383/389 locked Mac files unchanged and the same six pre-existing external differences. This work did not change the Mac app/settings/source, user-PC installation or public release.


- CI45 x64 is successful. Downloaded installed/direct title receipts complete five groups, and local-data receipts complete six, including title changes through real store refresh/rebuild/clear. Primary directly opened the installed task and project screenshots: Explicit/Desktop/Stored names and Alpha · Beta (+1) retain their own10/20/30/60 token counts without observed clipping. ARM64 for CI45 remains running; these results do not cover the newer pricing delta.
- Pricing migration now first reads schema without taking an extra write transaction when the field already exists. Only an actual migration takes a write lock and rechecks the schema under that lock. A test holds an unrelated write transaction while opening an already migrated repository; it completes before the writer is released. Build231/233 caught missing test cancellation tokens, fixed in build232/234.
- Independent review identified **QA-046 / Low**: the new copied-file theory changed file modification times, but the production scanner sorts paths. Both initial cases therefore exercised known-first only. The fixture now uses a-known.jsonl or z-known.jsonl around session.jsonl to exercise both real discovery orders. The original uncertain baseline and a copied exact request converge to one event with known context, preserve cache metadata through unknown→known→unknown DB imports, and estimate1.703 without duplicate usage. Final build235 passes with zero warnings/errors; Core52 passes **2,044 tests, 33 Windows-only skips, zero failures, 2,077 total**, including eleven high-context cases. Earlier11-pass/Core51 results alone did not prove both discovery orders. Final independent rereview and Windows pricing execution remain pending.

- Final independent pricing rereview marks the copied-order test finding resolved, confirms the current source and build235/Core52, and identifies no remaining source blocker in this delta. It separately checked CI45 x64 title receipts/screenshots; pricing native execution remains pending. Harness worker63/batch62 passes with no scope violations; this remains patch evidence, not formal ACCEPT.


## CI45 completion and register reconciliation

- CI45 (`36179678176`, `4b37dd5`) is fully successful on x64 and actual ARM64. ARM installed/direct host receipts report Arm64 OS/process and .NET10.0.12. Both title receipts complete five groups and both local-data receipts complete six. ARM MSI lifecycle records initial install, injected-upgrade rollback, upgrade, downgrade rejection, installed UI/pinned worker, uninstall preserving user data and signed-legacy refusal. The separate msi-handoff workflow job is skipped and is not claimed as passed.
- The difference register now assigns unique WP-32 through WP-36 entries to analytics dates, durable session metadata, desktop titles, high-context pricing and Claude agents. Earlier batch text accidentally reused the quota/reset/companion WP-21 through WP-24 numbers for unrelated analytics issues; those analytics references are corrected here. Existing quota/reset/companion entries and their evidence are unchanged. This is a traceability correction, not additional test coverage or an increased match percentage.


## CI46 x64 evidence and forty-seventh batch: continuous analytics lists

- CI46 (`36181235816`, `322fafb`) completes x64 Core/build/package, seven MSI lifecycle groups and installed/direct native UI. Both downloaded cost receipts complete thirteen groups, including known high-context pricing and ambiguous-context chart gaps. Primary directly opened the installed high-context PNG: total300100, ~$1.70 summary, populated cost bar and exact selected breakdown. ARM64 is still running. The UI fixture uses typed request metadata; scanner→database→estimator persistence is established by the separate Core regressions, not claimed as native end-to-end migration proof.
- **WP-37 / Medium.** UsagePane.Detail truncated projects/sessions at forty rows and required Show more. Actual read-only Mac ProjectsAnalyticsView/SessionsAnalyticsView enumerate every snapshot row; Settings owns the scrolling viewport. Windows now enumerates the full already-computed groups and removes the obsolete paging state from range, Find and Back. It retains the existing outer viewport, ordering, filters and row design. No new inner scrollbar, data cap or query contract is introduced. This difference is established by source flow; no pre-fix native failure is claimed.
- New NativeSmoke.AnalyticsLists mounts122 projects/121 recent sessions in an outer viewport, checks final-row bounds/focus, exact entity detail values and Back, full-list refresh, 7D/30D filtering, Find beyond row40 and360/650-width captures. It restores fixture data/settings independently after failure. This is a bounded long-list regression, not proof of arbitrary-size performance, actual physical keyboard/pointer input or every display scale. Build236 passes with zero warnings/errors. Core source is unchanged since the successful Core52 suite; the new native fixture and independent review remain pending.

- Independent review found **QA-047 / Medium** in the new regression: counting every descendant ScrollViewer also counted the Find TextBox's template content host. The assertion now walks each actual list row's ancestors and requires exactly the one outer viewport; a nested list viewport remains a failure while a text-editing control is not misclassified. Build237 passes with zero warnings/errors. This was a pre-execution test defect found by source review, not a product failure or previously passed native assertion.

- Independent review also identified **PERF-047 / Medium**: once paging was removed, SessionListDetail's per-row Count/FirstOrDefault over all session metadata produced quadratic dispatcher work. Mac builds direct-child counts once. Windows now builds the visible-child count map once per render and reads images from the existing metadata dictionary. The native fixture now supplies122 metadata records, verifies60 recent children versus61 in30D and whole-session images from outside the selected range, and instruments the metadata source to require exactly one enumeration during a list update. No measured latency claim is made. Build238 passes with zero warnings/errors; native execution and final rereview remain pending.

- Final independent source rereview marks both QA-047 and PERF-047 resolved, verifies build238 and records no new actionable source blocker. Harness worker67/batch66 passes with no scope violations. Long-list native execution is pending; no latency improvement percentage or full visual parity is inferred. CI46 is now fully successful on x64 and actual ARM64; the new ARM pricing artifacts are being retrieved separately before asserting their contents.


## CI46 completion and final checks for the continuous-list candidate

- CI46 (`36181235816`, `322fafb`) ARM artifacts have been read directly. Installed/direct hosts report Arm64 OS/process on .NET10.0.12; both cost receipts complete thirteen groups and MSI lifecycle completes seven. Primary and independent review opened installed/direct high-context PNGs and confirmed300100/~$1.70 with the expected breakdown. These artifacts do not validate the later continuous-list delta (`7d3ac5a`, CI47 `36183247683`). The separate msi-handoff job remains skipped.
- The six-project NuGet audit command, `dotnet list Windows/CodeRim.Windows.sln package --vulnerable --include-transitive --format json --no-restore`, exits0 with no reported vulnerable packages using the configured NuGet advisory source. This is known-package advisory coverage, not a claim that application code has no security defects. No dependency manifest or lockfile changes. Evidence: `/private/tmp/coderim-mac-parity-dependency-audit47.json`.
- Mac preservation check27 exactly equals check26:383 of389 locked files match the original baseline and the same six previously recorded external changes remain. This task adds no Mac changes. Harness summary returns **INCONCLUSIVE** (`summary-c807edb3543f4df9b435d7849a8edae1`); successful patch workers and independent native evidence are not formal ACCEPT.

- CI47 (`36183247683`, `7d3ac5a`) x64 job is successful. Downloaded installed/direct `windows-analytics-lists.json` each complete seven groups, including all122/121 rows, detail/Back, range/Find, final-row geometry and one metadata-source enumeration. MSI lifecycle completes seven groups. Primary directly opened installed narrow project/session end captures and the direct650-width session capture: Task000 retains1 token and Task120 retains121 with whole-session images, and neither final row is clipped. ARM64 execution remains in progress. Captures show a scrolled viewport, so a partially visible preceding row at the top is expected and is not full-content clipping.


## CI47 completion and remaining evidence limits

- CI47 (`36183247683`, product commit `7d3ac5a`) completes both x64 and native ARM64 jobs successfully. ARM Core log reports **2,077 succeeded, zero failed, zero skipped**. Downloaded installed/direct host receipts identify Arm64 OS/process. ARM MSI lifecycle completes the same seven install/rollback/upgrade/preservation groups. Primary opened installed session-list-end and direct project-list-end PNGs and confirmed the final row values and image metadata without observed clipping.
- The following receipt groups complete in all four executions (x64 installed/direct and ARM64 installed/direct). Counts are assertion groups, not independent requirements or a match-rate denominator.

| Receipt | Completed groups per execution |
| --- | ---: |
| Continuous analytics lists | 7 |
| Cost coverage and chart | 13 |
| Desktop task labels | 5 |
| Project/session details | 10 |
| Localized dates | 17 |
| Analytics state/navigation | 15 |
| Claude agent usage | 4 |
| Local data operations | 6 |

- **Actual pointer input is not fully verified.** x64 installed/direct pointer hover and native wheel receipts report PASS. ARM64 installed/direct pointer receipts report INCONCLUSIVE: WindowFromPoint identified the foreign WWAHost window at the target position; the native wheel fixture consequently submitted zero inputs. The ARM UI log has73 entries, including these two inconclusive records, so it must not be described as73 passes. WPF routed-handler/motion assertions and programmatic list scrolling/focus are separate evidence. Neither these results nor a successful workflow establish physical-PC mouse/DPI/multimonitor parity.
- The current `msi-handoff` job is skipped. The separate dispatch downloads the signed v2.1.10 draft; it was not run or relabeled as evidence for this candidate. No public release, signed update index or user's installed application is updated. Remaining comparison work includes Swift Charts automatic axes/continuous selection, the desktop account-switch lifecycle, other provider identity/routes and real-account/physical-display validation. Full parity and a100% completion rate remain unproven.

- Final independent ARM review confirms the receipt table, Core2077/0/0, MSI7, final-row PNGs and product source hashes bound to7d3ac5a. It separately records the WWAHost pointer obstruction and zero submitted wheel inputs, with no blanket73-pass claim. No new actionable source or sampled final-row rendering defect was found. Harness worker69 passes with no scope violations; the refreshed summary `summary-c581d8bb94d248a79520dcfa83b99469` remains INCONCLUSIVE.

## Windows notification-area menu reference (2026-09-26, native evidence pending)

The user's new screenshots request the Mac CodeRim status-item icon and menu on Windows. Mac `App/StatusItemController.swift` and `App/CodeRimMark.swift` remain the read-only reference. Windows uses its notification area (taskbar position is owned by the OS).

- **UI-TRAY-01 / Medium:** `Tray/TrayIconHost.cs` previously opened Usage immediately on left click and exposed Show notch/Open CodeRim/Refresh/Settings/Quit in a different order. Both mouse paths now expose Token Usage…, separator, checked Show Notch, Settings…, Check for Updates…, separator, Quit CodeRim. Windows Ctrl equivalents are shown; the dashboard also handles Ctrl+U/Ctrl+,/Ctrl+Q. The mark uses the reference's270° and48° ring segments,0.34 inner-radius ratio and1/18 inset. Application/executable branding resources are unchanged. Taskbar icon contrast follows system theme independently of app theme; high contrast uses system colours.
- **BUG-TRAY-02 / Medium:** Settings always called `Navigate("general")`, discarding the selected page and cancelling a mounted manual update. Reopening an existing window now restores/activates it without rebuilding its content. Explicit Usage navigation remains explicit. The update menu invokes the existing Information manual-update button, with the existing busy/closed/handoff guard and verification/restart flow. It does not use the automatic-check preference gate.
- **BUG-TRAY-03 / Medium (independent TRAY-01):** an external tray visibility change could leave the mounted Notch settings checkbox and dependent sections stale. `DashboardWindow` now synchronizes that checkbox, remembered Behaviour and section enablement in place, guarded against recursive settings writes. AppSettingsStore.ToggleNotch preserves hover/always-visible mode across hide/reveal and persistence. Failed writes leave the actual check/state intact and show a safe notification.
- Keyboard routing installs the documented [WPF/Windows Forms message interop](https://learn.microsoft.com/en-us/dotnet/desktop/wpf/advanced/windows-forms-and-wpf-interoperability-input-architecture). Popup activation follows the foreground dismissal requirement described in [Microsoft's tray-menu guidance](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-trackpopupmenu). Left-click activation does not invoke private NotifyIcon APIs.
- Build242 passes with zero warnings/errors, including recommended .NET analyzers. Initial build239 caught nullable renderer arguments; build240 caught a fixture enum typo and an analyzer warning. Both were corrected before the successful build. NativeSmoke.Tray adds actual application routing/persistence/view-identity assertions, failed-save regression, callback-only update/quit assertions, dark/light mounted menu captures, corner placement, an Escape message-queue test and five-size icon geometry checks. Native execution is pending; callback probes are not evidence of a real update installation, process exit or physical tray click.

- Independent review TRAY-01 prompted the mounted Settings synchronization above. QA-TRAY-01 strengthened Escape evidence: the popup must still be visible immediately before submission and its Closed event must report Keyboard, preventing an already-dismissed popup from falsely passing. Build243 passes with zero warnings/errors after this change. Physical tray interaction and real update installation remain separate pending evidence.
