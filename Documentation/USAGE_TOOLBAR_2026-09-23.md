# Windows Usage toolbar refinement — 2026-09-23

The user supplied a native Windows screenshot where Token Usage, Codex Limits and Refresh appeared as three bulky grey boxes. The macOS Usage toolbar is the visual reference; DESIGN.md remains authoritative.

Use one 246px-wide, 28px-high segmented group with equal-width 24px radio segments and native keyboard semantics. The tray has no additional outline; selection uses the shared accent and normal-weight type. Refresh becomes a 32px vector icon action retaining its accessible name, Ctrl+R tooltip and existing async/error behavior. The 32px provider picker uses a quiet outlined surface, paired native-style chevrons, a visible keyboard-focus outline the existing packaged provider glyph with semantic foreground colors; no new assets or dependencies are added. Dark, light and high-contrast colors come from the existing theme resources. The responsive two-row fallback remains for narrow widths.

The user's follow-up rejected the first compact pass. The second pass follows the actual macOS source's 246px segmented width, equalizes the two segments, removes the tray outline, uses normal-weight text, and replaces the generic provider picker with a scoped native-style template. Only this Usage toolbar changes. The wider action group switches to two rows below 480px to avoid collision with the provider picker.

Issues: **UI-010 / Low** — independent full-height action boxes obscured the mode relationship; a shared RadioButton group preserves exclusive selection and keyboard/UIA semantics. **UI-011 / Medium** — replacing DisplayMemberPath with a provider template disabled name-based selection; TextSearch.TextPath now explicitly targets Name. Recycled provider glyphs use a dependency property, and the smoke test checks the selected Claude logo after a routed TextInput event. **UI-012 / Medium** — the global TextBlock style overrode selected segment foreground in light mode; the control template explicitly binds foreground, and the native test checks the selected text against AccentText in all three themes. The detached-view PNG helper also now uses the theme's WindowBackground instead of an unconditional dark backing; the earlier width-360 capture is not readability evidence.

Verification: combined local build PASS with zero warnings/errors. First-pass dark/light/high-contrast minimum-size native captures and routed-input selection were inspected separately. Second-pass native x64 and ARM64 [CI 35839460586](https://github.com/dlfkdLR/CodeRim/actions/runs/35839460586), source `04b607b`, passed selected-state/contrast, routed TextInput provider selection and glyph, refresh, reflow and minimum-viewport checks. Actual installed dark/light/high-contrast PNGs were inspected; all three analytic links and footer fit inside the 840×560 window. A separate reviewer independently downloaded and verified the x64 artifact digest and compared all 337 evidence files byte-for-byte. Physical keyboard injection, a full screen-reader audit and the personal Windows installation are not claimed. Existing settings, provider data and updater logic are unchanged.
